/*
 * Project home: https://github.com/jaxio/celerio-angular-quickstart
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Modificado por CARLOS BAZALAR
 * Email: cbazalarlarosa@gmail.com
 * Template pack-angular:src/main/java/repository/EntityRepository.java.e.vm
 */
package com.incloud.hcp.repository;

import com.incloud.hcp.domain.MtrTipoFirma;
import com.incloud.hcp.domain.MtrTipoFirma_;
import com.incloud.hcp.repository._framework.JPACustomRepository;
import org.springframework.data.domain.Example;
import org.springframework.data.domain.ExampleMatcher;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.PageRequest;
import org.springframework.data.repository.NoRepositoryBean;
import org.springframework.data.repository.query.Param;
import org.springframework.data.rest.core.annotation.RestResource;

import java.util.List;

@NoRepositoryBean
public interface MtrTipoFirmaRepository extends JPACustomRepository<MtrTipoFirma, Integer> {

    /**
     * Return the persistent instance of {@link MtrTipoFirma} with the given unique property value codigoTipoFirma,
     * or null if there is no such persistent instance.
     *
     * @param codigoTipoFirma the unique value
     * @return the corresponding {@link MtrTipoFirma} persistent instance or null
     */
    @RestResource(path = "/getByCodigoTipoFirma")
    MtrTipoFirma getByCodigoTipoFirma(@Param("codigoTipoFirma") String codigoTipoFirma);

    /**
     * Return the persistent instance of {@link MtrTipoFirma} with the given unique property value descripcion,
     * or null if there is no such persistent instance.
     *
     * @param descripcion the unique value
     * @return the corresponding {@link MtrTipoFirma} persistent instance or null
     */
    @RestResource(path = "/getByDescripcion")
    MtrTipoFirma getByDescripcion(@Param("descripcion") String descripcion);

    default List<MtrTipoFirma> findCompletePaginated(String query, int maxResults) {
        MtrTipoFirma probe = new MtrTipoFirma();
        //probe.setCodigoTipoFirma(query);
        probe.setCodigoTipoFirma(null);
        ExampleMatcher matcher = ExampleMatcher.matching() //
                .withMatcher(MtrTipoFirma_.codigoTipoFirma.getName(), match -> match.ignoreCase().startsWith());

        Page<MtrTipoFirma> page = this.findAll(Example.of(probe, matcher), PageRequest.of(0, maxResults));
        return page.getContent();
    }

}
