/*
 * Project home: https://github.com/jaxio/celerio-angular-quickstart
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Modificado por CARLOS BAZALAR
 * Email: cbazalarlarosa@gmail.com
 * Template pack-angular:src/main/java/repository/EntityRepository.java.e.vm
 */
package com.incloud.hcp.repository;

import com.incloud.hcp.domain.MtrAprobador;
import com.incloud.hcp.domain.MtrAprobador_;
import com.incloud.hcp.domain.MtrAreaTrabajo;
import com.incloud.hcp.repository._framework.JPACustomRepository;
import org.springframework.data.domain.Example;
import org.springframework.data.domain.ExampleMatcher;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.PageRequest;
import org.springframework.data.repository.NoRepositoryBean;
import org.springframework.data.repository.query.Param;
import org.springframework.data.rest.core.annotation.RestResource;

import java.util.List;

@NoRepositoryBean
public interface MtrAprobadorRepository extends JPACustomRepository<MtrAprobador, Integer> {

    /**
     * Return the persistent instance of {@link MtrAprobador} with the given unique property value aprobadorSap,
     * or null if there is no such persistent instance.
     *
     * @param aprobadorSap the unique value
     * @return the corresponding {@link MtrAprobador} persistent instance or null
     */
    @RestResource(path = "/getByAprobadorSap")
    MtrAprobador getByAprobadorSap(@Param("aprobadorSap") String aprobadorSap);

    /**
     * Return the persistent instance of {@link MtrAprobador} with the given unique property value email,
     * or null if there is no such persistent instance.
     *
     * @param email the unique value
     * @return the corresponding {@link MtrAprobador} persistent instance or null
     */
    @RestResource(path = "/getByEmail")
    MtrAprobador getByEmail(@Param("email") String email);

    default List<MtrAprobador> findCompletePaginated(String query, int maxResults) {
        MtrAprobador probe = new MtrAprobador();
        //probe.setAprobadorSap(query);
        probe.setAprobadorSap(null);
        ExampleMatcher matcher = ExampleMatcher.matching() //
                .withMatcher(MtrAprobador_.aprobadorSap.getName(), match -> match.ignoreCase().startsWith());

        Page<MtrAprobador> page = this.findAll(Example.of(probe, matcher), PageRequest.of(0, maxResults));
        return page.getContent();
    }

    Long countByMtrAreaTrabajo(@Param("mtrAreaTrabajo") MtrAreaTrabajo mtrAreaTrabajo);

}
