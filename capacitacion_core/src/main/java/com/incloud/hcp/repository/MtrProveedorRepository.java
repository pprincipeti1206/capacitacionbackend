/*
 * Project home: https://github.com/jaxio/celerio-angular-quickstart
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Modificado por CARLOS BAZALAR
 * Email: cbazalarlarosa@gmail.com
 * Template pack-angular:src/main/java/repository/EntityRepository.java.e.vm
 */
package com.incloud.hcp.repository;

import com.incloud.hcp.domain.MtrProveedor;
import com.incloud.hcp.domain.MtrProveedor_;
import com.incloud.hcp.repository._framework.JPACustomRepository;
import org.springframework.data.domain.Example;
import org.springframework.data.domain.ExampleMatcher;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.PageRequest;
import org.springframework.data.repository.NoRepositoryBean;
import org.springframework.data.repository.query.Param;
import org.springframework.data.rest.core.annotation.RestResource;

import java.util.List;

@NoRepositoryBean
public interface MtrProveedorRepository extends JPACustomRepository<MtrProveedor, Integer> {

    /**
     * Return the persistent instance of {@link MtrProveedor} with the given unique property value ruc,
     * or null if there is no such persistent instance.
     *
     * @param ruc the unique value
     * @return the corresponding {@link MtrProveedor} persistent instance or null
     */
    @RestResource(path = "/getByRuc")
    MtrProveedor getByRuc(@Param("ruc") String ruc);

    default List<MtrProveedor> findCompletePaginated(String query, int maxResults) {
        MtrProveedor probe = new MtrProveedor();
        //probe.setRuc(query);
        probe.setRuc(null);
        ExampleMatcher matcher = ExampleMatcher.matching() //
                .withMatcher(MtrProveedor_.ruc.getName(), match -> match.ignoreCase().startsWith());

        Page<MtrProveedor> page = this.findAll(Example.of(probe, matcher), PageRequest.of(0, maxResults));
        return page.getContent();
    }

}
