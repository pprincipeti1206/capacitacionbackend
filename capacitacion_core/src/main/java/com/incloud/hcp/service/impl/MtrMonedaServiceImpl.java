/*
 * Project home: https://github.com/jaxio/celerio-angular-quickstart
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Modificado por CARLOS BAZALAR
 * Email: cbazalarlarosa@gmail.com
 * Template pack-angular:src/main/java/dto/EntityDTOService.java.e.vm
 */
package com.incloud.hcp.service.impl;

import com.incloud.hcp.common.enums.AppParametriaLabelEnum;
import com.incloud.hcp.common.enums.AppParametriaModuloEnum;
import com.incloud.hcp.common.excel.ExcelDefault;
import com.incloud.hcp.domain.AppParametria;
import com.incloud.hcp.domain.MtrMoneda;
import com.incloud.hcp.domain.MtrMoneda_;
import com.incloud.hcp.domain.response.MtrMonedaResponse;
import com.incloud.hcp.exception.ServiceException;
import com.incloud.hcp.repository.delta.AppParametriaDeltaRepository;
import com.incloud.hcp.repository.delta.MtrMonedaDeltaRepository;
import com.incloud.hcp.service.MtrMonedaService;
import com.incloud.hcp.service._framework.bean.BeanCargaMasivoDTO;
import com.incloud.hcp.service._framework.bean.BeanListaMasivoDTO;
import com.incloud.hcp.service._framework.impl.JPACustomServiceImpl;
import com.incloud.hcp.service.requireNew.MtrMonedaRequireNewService;
import com.incloud.hcp.service.support.PageRequestByExample;
import com.incloud.hcp.utils.Constants;
import com.incloud.hcp.utils.PredicateUtils;
import org.apache.commons.beanutils.BeanUtils;
import org.apache.commons.lang.StringUtils;
import org.apache.poi.hssf.usermodel.HSSFRow;
import org.apache.poi.ss.usermodel.Cell;
import org.apache.poi.ss.usermodel.CellStyle;
import org.apache.poi.ss.usermodel.Row;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.i18n.LocaleContextHolder;
import org.springframework.data.domain.Example;
import org.springframework.data.domain.ExampleMatcher;
import org.springframework.data.domain.Sort;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Propagation;
import org.springframework.transaction.annotation.Transactional;

import javax.persistence.criteria.CriteriaBuilder;
import javax.persistence.criteria.Predicate;
import javax.persistence.criteria.Root;
import java.math.BigDecimal;
import java.util.ArrayList;
import java.util.List;
import java.util.Optional;

/**
 * A simple DTO Facility for MtrMoneda.
 */
@Service
@Transactional(propagation = Propagation.REQUIRED, rollbackFor = Exception.class)
public abstract class MtrMonedaServiceImpl extends JPACustomServiceImpl<MtrMonedaResponse, MtrMoneda, Integer> implements MtrMonedaService {

    protected final String NAME_SHEET = "MtrMoneda";
    protected final String CONFIG_TITLE = "com/incloud/hcp/excel/MtrMonedaExcel.xml";
    private final Integer REGISTROS_COLOR = 10;

    @Autowired
    protected AppParametriaDeltaRepository appParametriaDeltaRepository;

    @Autowired
    protected MtrMonedaDeltaRepository mtrMonedaDeltaRepository;

    @Autowired
    protected MtrMonedaRequireNewService mtrMonedaRequireNewService;

    /*****************************/
    /* Metodos de Busqueda       */
    /*****************************/

    protected final ExampleMatcher setAbstractFind(ExampleMatcher matcher) {
        matcher = ExampleMatcher.matching() //
                .withMatcher(MtrMoneda_.sigla.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(MtrMoneda_.monedaDescrip.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(MtrMoneda_.descBrv.getName(), match -> match.ignoreCase().startsWith());
        return matcher;
    }

    protected abstract Sort setFindAll(Sort sort);

    protected abstract Sort setFind(MtrMoneda req, ExampleMatcher matcher, Example<MtrMoneda> example, Sort sort);

    protected abstract void setFindPaginated(PageRequestByExample<MtrMoneda> req, ExampleMatcher matcher, Example<MtrMoneda> example);

    protected final MtrMoneda setObtenerBeanResponse(MtrMonedaResponse bean) {
        return bean.getBean();
    }

    protected final Class<MtrMoneda> setObtenerClassBean() {
        return MtrMoneda.class;
    }

    protected List<Predicate> setAbstractPredicate(MtrMonedaResponse bean, CriteriaBuilder cb, Root<MtrMoneda> root) {
        List<Predicate> predicates = new ArrayList<>();
        MtrMoneda entity = bean.getBean();
        /* Obtener condiciones */
        PredicateUtils.addPredicates(predicates, bean.getIdCondicion(), "id", entity.getId(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getSiglaCondicion(), "sigla", entity.getSigla(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getMonedaDescripCondicion(), "monedaDescrip", entity.getMonedaDescrip(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getDescBrvCondicion(), "descBrv", entity.getDescBrv(), cb, root);
        /* Obtener valores de Lista */
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "id", bean.getIdList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "sigla", bean.getSiglaList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "monedaDescrip", bean.getMonedaDescripList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "descBrv", bean.getDescBrvList(), cb, root);
        return predicates;
    }

    /****************/
    /* METODOS CRUD */
    /****************/

    protected final String validacionesPrevias(MtrMoneda bean) throws Exception {
        String mensaje = "";
        if (!Optional.of(bean.getSigla()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mtrMoneda.sigla.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        if (!Optional.of(bean.getMonedaDescrip()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mtrMoneda.monedaDescrip.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        if (!Optional.of(bean.getDescBrv()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mtrMoneda.descBrv.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        String msg = this.setValidacionesPrevias(bean);
        if (StringUtils.isNotBlank(msg)) {
            mensaje += "* " + msg + "<br/>";
        }
        return mensaje;
    }

    protected String setValidacionesPrevias(MtrMoneda bean) throws Exception {
        String mensaje = "";
        return mensaje;
    }

    protected String validacionesPreviasCreate(MtrMoneda bean) throws Exception {
        String msg = null;
        MtrMoneda validar = null;
        return msg;
    }

    protected String validacionesPreviasSave(MtrMoneda bean) throws Exception {
        String msg = null;
        MtrMoneda validar = null;
        return msg;
    }

    protected MtrMoneda completarDatosBean(MtrMoneda bean) throws Exception {
        BigDecimal data = new BigDecimal(0.00);
        bean = this.setCompletarDatosBean(bean);
        return bean;
    }

    protected MtrMoneda setCompletarDatosBean(MtrMoneda bean) throws Exception {
        return bean;
    }

    protected final MtrMoneda setAbstractCreate(MtrMoneda dto) throws Exception {
        MtrMoneda bean = new MtrMoneda();
        bean = (MtrMoneda) BeanUtils.cloneBean(dto);
        return bean;
    }

    /************************/
    /* METODOS CRUD Masivos */
    /************************/

    protected MtrMoneda setUploadExcel(Cell currentCell, MtrMoneda mtrMoneda, int contador) throws Exception {
        Double valorDouble = new Double(0);
        BigDecimal valorDecimal = new BigDecimal(0);
        Boolean valorBoolean = new Boolean(true);
        String valorCadena = "";
        switch (contador) {
        case 1:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 10) {
                    throw new ServiceException("Valor Campo sigla contiene mas de 10 caracter(es)");
                }
                mtrMoneda.setSigla(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo sigla está en formato incorrecto");
            }
            break;
        case 2:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 10) {
                    throw new ServiceException("Valor Campo monedaDescrip contiene mas de 10 caracter(es)");
                }
                mtrMoneda.setMonedaDescrip(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo monedaDescrip está en formato incorrecto");
            }
            break;
        case 3:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 4) {
                    throw new ServiceException("Valor Campo descBrv contiene mas de 4 caracter(es)");
                }
                mtrMoneda.setDescBrv(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo descBrv está en formato incorrecto");
            }
            break;
        default:
            break;
        }
        return mtrMoneda;
    }

    protected AppParametria setObtenerRegistroConfiguracionUploadExcel() {
        AppParametria appParametriaData = this.appParametriaDeltaRepository.getByModuloAndLabelAndStatus(AppParametriaModuloEnum.CARGA_EXCEL.getEstado(),
                AppParametriaLabelEnum.INICIO_REGISTRO_DATA.getEstado(), Constants.ESTADO_ACTIVO);
        return appParametriaData;
    }

    protected MtrMoneda setInicializarBeanUpdateExcel() {
        MtrMoneda bean = new MtrMoneda();
        bean.setId(null);
        return bean;
    }

    protected final Integer setObtenerId(MtrMoneda bean) {
        return bean.getId();
    }

    /************************/
    /* Instancia de Bean    */
    /************************/

    protected final MtrMoneda createInstance() {
        MtrMoneda mtrMoneda = new MtrMoneda();
        return mtrMoneda;
    }

    protected final BeanCargaMasivoDTO<MtrMoneda> createInstanceMasivoDTO() {
        BeanCargaMasivoDTO<MtrMoneda> beanCargaMasivoDTO = new BeanCargaMasivoDTO<MtrMoneda>();
        return beanCargaMasivoDTO;

    }

    protected final BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrMoneda>> createInstanceListaMasivoDTO() {
        BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrMoneda>> beanCargaMasivoDTOBeanListaMasivoDTO = new BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrMoneda>>();
        return beanCargaMasivoDTOBeanListaMasivoDTO;
    }

    /*****************************/
    /* Metodos que generan Excel */
    /*****************************/

    protected String devuelveNombreSheet() {
        return this.NAME_SHEET;
    }

    protected String devuelveListaHeaderExcelXML() {
        return this.CONFIG_TITLE;
    }

    protected int setAbstractDownloadExcel(MtrMoneda bean, HSSFRow dataRow) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getSigla(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getMonedaDescrip(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getDescBrv(), dataRow.createCell(contador));
        contador++;
        return contador;
    }

    protected int setAbstractDownloadExcelSXLSX(MtrMoneda bean, Row dataRow, List<CellStyle> cellStyleList) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador), "I", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getSigla(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getMonedaDescrip(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getDescBrv(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        return contador;
    }

    protected String setAbstractGenerateInsertExcelSXLSX(MtrMoneda bean) {
        String fechaS = "";
        String sqlInsert = "INSERT INTO mtr_moneda(";
        sqlInsert = sqlInsert + "mtr_moneda_id" + ", ";
        sqlInsert = sqlInsert + "sigla" + ", ";
        sqlInsert = sqlInsert + "moneda_descrip" + ", ";
        sqlInsert = sqlInsert + "desc_brv" + ")";
        sqlInsert = sqlInsert + " VALUES (";
        sqlInsert = sqlInsert + bean.getId() + ", ";
        if (StringUtils.isBlank(bean.getSigla())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getSigla() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getMonedaDescrip())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getMonedaDescrip() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getDescBrv())) {
            sqlInsert = sqlInsert + "null";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getDescBrv() + "'";
        }
        sqlInsert = sqlInsert + " );";
        return sqlInsert;
    }

    /*****************/
    /* Otros Metodos */
    /*****************/

}
