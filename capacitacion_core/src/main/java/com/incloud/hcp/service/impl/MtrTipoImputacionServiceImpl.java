/*
 * Project home: https://github.com/jaxio/celerio-angular-quickstart
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Modificado por CARLOS BAZALAR
 * Email: cbazalarlarosa@gmail.com
 * Template pack-angular:src/main/java/dto/EntityDTOService.java.e.vm
 */
package com.incloud.hcp.service.impl;

import com.incloud.hcp.common.enums.AppParametriaLabelEnum;
import com.incloud.hcp.common.enums.AppParametriaModuloEnum;
import com.incloud.hcp.common.excel.ExcelDefault;
import com.incloud.hcp.domain.AppParametria;
import com.incloud.hcp.domain.MtrTipoImputacion;
import com.incloud.hcp.domain.MtrTipoImputacion_;
import com.incloud.hcp.domain.response.MtrTipoImputacionResponse;
import com.incloud.hcp.exception.ServiceException;
import com.incloud.hcp.repository.delta.AppParametriaDeltaRepository;
import com.incloud.hcp.repository.delta.MtrTipoImputacionDeltaRepository;
import com.incloud.hcp.service.MtrTipoImputacionService;
import com.incloud.hcp.service._framework.bean.BeanCargaMasivoDTO;
import com.incloud.hcp.service._framework.bean.BeanListaMasivoDTO;
import com.incloud.hcp.service._framework.impl.JPACustomServiceImpl;
import com.incloud.hcp.service.requireNew.MtrTipoImputacionRequireNewService;
import com.incloud.hcp.service.support.PageRequestByExample;
import com.incloud.hcp.utils.Constants;
import com.incloud.hcp.utils.PredicateUtils;
import org.apache.commons.beanutils.BeanUtils;
import org.apache.commons.lang.StringUtils;
import org.apache.poi.hssf.usermodel.HSSFRow;
import org.apache.poi.ss.usermodel.Cell;
import org.apache.poi.ss.usermodel.CellStyle;
import org.apache.poi.ss.usermodel.Row;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.i18n.LocaleContextHolder;
import org.springframework.data.domain.Example;
import org.springframework.data.domain.ExampleMatcher;
import org.springframework.data.domain.Sort;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Propagation;
import org.springframework.transaction.annotation.Transactional;

import javax.persistence.criteria.CriteriaBuilder;
import javax.persistence.criteria.Predicate;
import javax.persistence.criteria.Root;
import java.math.BigDecimal;
import java.util.ArrayList;
import java.util.List;
import java.util.Optional;

/**
 * A simple DTO Facility for MtrTipoImputacion.
 */
@Service
@Transactional(propagation = Propagation.REQUIRED, rollbackFor = Exception.class)
public abstract class MtrTipoImputacionServiceImpl extends JPACustomServiceImpl<MtrTipoImputacionResponse, MtrTipoImputacion, Integer>
        implements MtrTipoImputacionService {

    protected final String NAME_SHEET = "MtrTipoImputacion";
    protected final String CONFIG_TITLE = "com/incloud/hcp/excel/MtrTipoImputacionExcel.xml";
    private final Integer REGISTROS_COLOR = 10;

    @Autowired
    protected AppParametriaDeltaRepository appParametriaDeltaRepository;

    @Autowired
    protected MtrTipoImputacionDeltaRepository mtrTipoImputacionDeltaRepository;

    @Autowired
    protected MtrTipoImputacionRequireNewService mtrTipoImputacionRequireNewService;

    /*****************************/
    /* Metodos de Busqueda       */
    /*****************************/

    protected final ExampleMatcher setAbstractFind(ExampleMatcher matcher) {
        matcher = ExampleMatcher.matching() //
                .withMatcher(MtrTipoImputacion_.codigoTipoImputacion.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(MtrTipoImputacion_.descripcion.getName(), match -> match.ignoreCase().startsWith());
        return matcher;
    }

    protected abstract Sort setFindAll(Sort sort);

    protected abstract Sort setFind(MtrTipoImputacion req, ExampleMatcher matcher, Example<MtrTipoImputacion> example, Sort sort);

    protected abstract void setFindPaginated(PageRequestByExample<MtrTipoImputacion> req, ExampleMatcher matcher, Example<MtrTipoImputacion> example);

    protected final MtrTipoImputacion setObtenerBeanResponse(MtrTipoImputacionResponse bean) {
        return bean.getBean();
    }

    protected final Class<MtrTipoImputacion> setObtenerClassBean() {
        return MtrTipoImputacion.class;
    }

    protected List<Predicate> setAbstractPredicate(MtrTipoImputacionResponse bean, CriteriaBuilder cb, Root<MtrTipoImputacion> root) {
        List<Predicate> predicates = new ArrayList<>();
        MtrTipoImputacion entity = bean.getBean();
        /* Obtener condiciones */
        PredicateUtils.addPredicates(predicates, bean.getIdCondicion(), "id", entity.getId(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getCodigoTipoImputacionCondicion(), "codigoTipoImputacion", entity.getCodigoTipoImputacion(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getDescripcionCondicion(), "descripcion", entity.getDescripcion(), cb, root);
        /* Obtener valores de Lista */
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "id", bean.getIdList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "codigoTipoImputacion", bean.getCodigoTipoImputacionList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "descripcion", bean.getDescripcionList(), cb, root);
        return predicates;
    }

    /****************/
    /* METODOS CRUD */
    /****************/

    protected final String validacionesPrevias(MtrTipoImputacion bean) throws Exception {
        String mensaje = "";
        if (!Optional.of(bean.getCodigoTipoImputacion()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mtrTipoImputacion.codigoTipoImputacion.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        if (!Optional.of(bean.getDescripcion()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mtrTipoImputacion.descripcion.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        String msg = this.setValidacionesPrevias(bean);
        if (StringUtils.isNotBlank(msg)) {
            mensaje += "* " + msg + "<br/>";
        }
        return mensaje;
    }

    protected String setValidacionesPrevias(MtrTipoImputacion bean) throws Exception {
        String mensaje = "";
        return mensaje;
    }

    protected String validacionesPreviasCreate(MtrTipoImputacion bean) throws Exception {
        String msg = null;
        MtrTipoImputacion validar = null;
        validar = this.mtrTipoImputacionDeltaRepository.getByCodigoTipoImputacion(bean.getCodigoTipoImputacion());
        if (Optional.ofNullable(validar).isPresent()) {
            msg = this.messageSource.getMessage("message.mtrTipoImputacion.codigoTipoImputacion.duplicado", null, LocaleContextHolder.getLocale());
            return msg;
        }
        validar = this.mtrTipoImputacionDeltaRepository.getByDescripcion(bean.getDescripcion());
        if (Optional.ofNullable(validar).isPresent()) {
            msg = this.messageSource.getMessage("message.mtrTipoImputacion.descripcion.duplicado", null, LocaleContextHolder.getLocale());
            return msg;
        }
        return msg;
    }

    protected String validacionesPreviasSave(MtrTipoImputacion bean) throws Exception {
        String msg = null;
        MtrTipoImputacion validar = null;
        validar = this.mtrTipoImputacionDeltaRepository.getByCodigoTipoImputacion(bean.getCodigoTipoImputacion());
        if (Optional.ofNullable(validar).isPresent()) {
            if (bean.getId().intValue() != validar.getId().intValue()) {
                msg = this.messageSource.getMessage("message.mtrTipoImputacion.codigoTipoImputacion.duplicado", null, LocaleContextHolder.getLocale());
                return msg;
            }
        }
        validar = this.mtrTipoImputacionDeltaRepository.getByDescripcion(bean.getDescripcion());
        if (Optional.ofNullable(validar).isPresent()) {
            if (bean.getId().intValue() != validar.getId().intValue()) {
                msg = this.messageSource.getMessage("message.mtrTipoImputacion.descripcion.duplicado", null, LocaleContextHolder.getLocale());
                return msg;
            }
        }
        return msg;
    }

    protected MtrTipoImputacion completarDatosBean(MtrTipoImputacion bean) throws Exception {
        BigDecimal data = new BigDecimal(0.00);
        bean = this.setCompletarDatosBean(bean);
        return bean;
    }

    protected MtrTipoImputacion setCompletarDatosBean(MtrTipoImputacion bean) throws Exception {
        return bean;
    }

    protected final MtrTipoImputacion setAbstractCreate(MtrTipoImputacion dto) throws Exception {
        MtrTipoImputacion bean = new MtrTipoImputacion();
        bean = (MtrTipoImputacion) BeanUtils.cloneBean(dto);
        return bean;
    }

    /************************/
    /* METODOS CRUD Masivos */
    /************************/

    protected MtrTipoImputacion setUploadExcel(Cell currentCell, MtrTipoImputacion mtrTipoImputacion, int contador) throws Exception {
        Double valorDouble = new Double(0);
        BigDecimal valorDecimal = new BigDecimal(0);
        Boolean valorBoolean = new Boolean(true);
        String valorCadena = "";
        switch (contador) {
        case 1:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 5) {
                    throw new ServiceException("Valor Campo codigoTipoImputacion contiene mas de 5 caracter(es)");
                }
                mtrTipoImputacion.setCodigoTipoImputacion(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo codigoTipoImputacion está en formato incorrecto");
            }
            break;
        case 2:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 100) {
                    throw new ServiceException("Valor Campo descripcion contiene mas de 100 caracter(es)");
                }
                mtrTipoImputacion.setDescripcion(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo descripcion está en formato incorrecto");
            }
            break;
        default:
            break;
        }
        return mtrTipoImputacion;
    }

    protected AppParametria setObtenerRegistroConfiguracionUploadExcel() {
        AppParametria appParametriaData = this.appParametriaDeltaRepository.getByModuloAndLabelAndStatus(AppParametriaModuloEnum.CARGA_EXCEL.getEstado(),
                AppParametriaLabelEnum.INICIO_REGISTRO_DATA.getEstado(), Constants.ESTADO_ACTIVO);
        return appParametriaData;
    }

    protected MtrTipoImputacion setInicializarBeanUpdateExcel() {
        MtrTipoImputacion bean = new MtrTipoImputacion();
        bean.setId(null);
        return bean;
    }

    protected final Integer setObtenerId(MtrTipoImputacion bean) {
        return bean.getId();
    }

    /************************/
    /* Instancia de Bean    */
    /************************/

    protected final MtrTipoImputacion createInstance() {
        MtrTipoImputacion mtrTipoImputacion = new MtrTipoImputacion();
        return mtrTipoImputacion;
    }

    protected final BeanCargaMasivoDTO<MtrTipoImputacion> createInstanceMasivoDTO() {
        BeanCargaMasivoDTO<MtrTipoImputacion> beanCargaMasivoDTO = new BeanCargaMasivoDTO<MtrTipoImputacion>();
        return beanCargaMasivoDTO;

    }

    protected final BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrTipoImputacion>> createInstanceListaMasivoDTO() {
        BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrTipoImputacion>> beanCargaMasivoDTOBeanListaMasivoDTO = new BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrTipoImputacion>>();
        return beanCargaMasivoDTOBeanListaMasivoDTO;
    }

    /*****************************/
    /* Metodos que generan Excel */
    /*****************************/

    protected String devuelveNombreSheet() {
        return this.NAME_SHEET;
    }

    protected String devuelveListaHeaderExcelXML() {
        return this.CONFIG_TITLE;
    }

    protected int setAbstractDownloadExcel(MtrTipoImputacion bean, HSSFRow dataRow) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getCodigoTipoImputacion(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getDescripcion(), dataRow.createCell(contador));
        contador++;
        return contador;
    }

    protected int setAbstractDownloadExcelSXLSX(MtrTipoImputacion bean, Row dataRow, List<CellStyle> cellStyleList) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador), "I", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getCodigoTipoImputacion(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getDescripcion(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        return contador;
    }

    protected String setAbstractGenerateInsertExcelSXLSX(MtrTipoImputacion bean) {
        String fechaS = "";
        String sqlInsert = "INSERT INTO mtr_tipo_imputacion(";
        sqlInsert = sqlInsert + "mtr_tipo_imputacion_id" + ", ";
        sqlInsert = sqlInsert + "codigo_tipo_imputacion" + ", ";
        sqlInsert = sqlInsert + "descripcion" + ")";
        sqlInsert = sqlInsert + " VALUES (";
        sqlInsert = sqlInsert + bean.getId() + ", ";
        if (StringUtils.isBlank(bean.getCodigoTipoImputacion())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getCodigoTipoImputacion() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getDescripcion())) {
            sqlInsert = sqlInsert + "null";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getDescripcion() + "'";
        }
        sqlInsert = sqlInsert + " );";
        return sqlInsert;
    }

    /*****************/
    /* Otros Metodos */
    /*****************/

}
