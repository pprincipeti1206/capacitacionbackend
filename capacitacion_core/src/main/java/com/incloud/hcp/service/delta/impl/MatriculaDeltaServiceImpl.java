/*
 * Project home: https://github.com/jaxio/celerio-angular-quickstart
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Modificado por CARLOS BAZALAR
 * Email: cbazalarlarosa@gmail.com
 * Template pack-angular:src/main/java/dto/EntitydeltaDTOService.java.e.vm
 */
package com.incloud.hcp.service.delta.impl;

import com.incloud.hcp.domain.Horario;
import com.incloud.hcp.domain.Matricula;
import com.incloud.hcp.domain.Participante;
import com.incloud.hcp.domain.response.MatriculaResponse;
import com.incloud.hcp.service.delta.MatriculaDeltaService;
import com.incloud.hcp.service.impl.MatriculaServiceImpl;
import com.incloud.hcp.service.support.PageRequestByExample;
import org.apache.poi.hssf.usermodel.HSSFRow;
import org.apache.poi.hssf.usermodel.HSSFSheet;
import org.apache.poi.ss.usermodel.Cell;
import org.springframework.data.domain.Example;
import org.springframework.data.domain.ExampleMatcher;
import org.springframework.data.domain.Sort;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Propagation;
import org.springframework.transaction.annotation.Transactional;

import javax.persistence.criteria.CriteriaBuilder;
import javax.persistence.criteria.CriteriaQuery;
import javax.persistence.criteria.Predicate;
import javax.persistence.criteria.Root;
import java.util.List;

/**
 * A simple DTO Facility for Matricula.
 */
@Service
@Transactional(propagation = Propagation.REQUIRED, rollbackFor = Exception.class)
public class MatriculaDeltaServiceImpl extends MatriculaServiceImpl implements MatriculaDeltaService {

    /**************************/
    /* Metodos Personalizados */
    /**************************/

    /***********************/
    /* Metodos de Busqueda */
    /***********************/

    protected Sort setFindAll(Sort sort) {
        return sort;
    }

    protected Sort setFind(Matricula req, ExampleMatcher matcher, Example<Matricula> example, Sort sort) {
        return sort;
    }

    protected void setFindPaginated(PageRequestByExample<Matricula> req, ExampleMatcher matcher, Example<Matricula> example) {
        return;
    }

    protected List<Predicate> setAdicionalDeltaPredicate(List<Predicate> predicates, MatriculaResponse bean, CriteriaBuilder cb, CriteriaQuery<Matricula> query,
            Root<Matricula> root) throws Exception {

        Matricula entity = bean.getBean();
        //Ejemplo
        /*
        if (Optional.ofNullable(entity.get<VariableManytoOne>()).isPresent()) {
            Join<Matricula, <ClaseManytoOne>> from<ClaseManytoOne> = countRoot.join("<variableManytoOne>", JoinType.INNER);
        
            if (Optional.ofNullable(entity.get<VariableManytoOne>().get<Atributo>()).isPresent()) {
                Join<Matricula, <ClaseManytoOne>> from<ClaseManytoOne> = countRoot.join("<variableManytoOne>", JoinType.INNER);
                Predicate thirdCondition = cb.equal(from<ClaseManytoOne>.get("<Atributo>"), entity.get<ClaseManytoOne>().get<Atributo>());
                predicates.add(thirdCondition);
            }
            
        }
        query.orderBy(cb.desc(root.get("<campo entity>")));
        */
        return predicates;
    }

    protected Root<Matricula> setAdicionalDeltaTotalPredicate(MatriculaResponse bean, Root<Matricula> countRoot) throws Exception {
        Matricula entity = bean.getBean();
        //Ejemplo
        /*
        if (Optional.ofNullable(entity.get<VariableManytoOne>()).isPresent()) {
            if (Optional.ofNullable(entity.get<VariableManytoOne>()).isPresent()) {
                Join<Matricula, <ClaseManytoOne>> from<ClaseManytoOne> = countRoot.join("<variableManytoOne>", JoinType.INNER);
            }
        }
        */
        return countRoot;
    }

    /****************/
    /* METODOS CRUD */
    /****************/

    protected String setValidacionesPrevias(Matricula bean) throws Exception {
        String mensaje = "";
        return mensaje;
    }

    protected Matricula setCreate(Matricula bean) throws Exception {
        return bean;
    }

    protected void setSave(Matricula dto) throws Exception {
        return;
    }

    protected void setDelete(Integer id) throws Exception {

    }

    protected void setDeleteAll() throws Exception {

    }

    /************************/
    /* METODOS CRUD Masivos */
    /************************/

    protected Matricula setUploadExcel(Cell currentCell, Matricula matricula, int contador) throws Exception {
        matricula = super.setUploadExcel(currentCell, matricula, contador);
        return matricula;
    }

    protected String setSaveMasivo(Matricula dto) throws Exception {
        return "";
    }

    protected List<Matricula> setBeforeDeleteMasivo(List<Matricula> listaDto) throws Exception {
        return listaDto;
    }

    protected void setAfterDeleteMasivo() throws Exception {
        return;
    }

    /*****************************/
    /* Metodos que generan Excel */
    /*****************************/

    protected void setDownloadExcelItem(Matricula bean, HSSFRow dataRow) {

    }

    protected void setDownloadExcel(HSSFSheet sheet) {

    }

    /*****************/
    /* Otros Metodos */
    /*****************/

    protected String setGraphDescripcionByHorario(Horario horario) {
        return horario.getId().toString();
    }

    protected String setGraphPieChartTituloByHorario() {
        return "Horario";
    }

    protected String setGraphBarChartTituloByHorario() {
        return "Horario";
    }

    protected String setGraphDescripcionByParticipante(Participante participante) {
        return participante.getId().toString();
    }

    protected String setGraphPieChartTituloByParticipante() {
        return "Participante";
    }

    protected String setGraphBarChartTituloByParticipante() {
        return "Participante";
    }

}
