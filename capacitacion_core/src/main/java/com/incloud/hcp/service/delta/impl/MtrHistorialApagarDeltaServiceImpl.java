/*
 * Project home: https://github.com/jaxio/celerio-angular-quickstart
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Modificado por CARLOS BAZALAR
 * Email: cbazalarlarosa@gmail.com
 * Template pack-angular:src/main/java/dto/EntitydeltaDTOService.java.e.vm
 */
package com.incloud.hcp.service.delta.impl;

import com.incloud.hcp.domain.MtrHistorialApagar;
import com.incloud.hcp.domain.response.MtrHistorialApagarResponse;
import com.incloud.hcp.service.delta.MtrHistorialApagarDeltaService;
import com.incloud.hcp.service.impl.MtrHistorialApagarServiceImpl;
import com.incloud.hcp.service.support.PageRequestByExample;
import org.apache.poi.hssf.usermodel.HSSFRow;
import org.apache.poi.hssf.usermodel.HSSFSheet;
import org.apache.poi.ss.usermodel.Cell;
import org.springframework.data.domain.Example;
import org.springframework.data.domain.ExampleMatcher;
import org.springframework.data.domain.Sort;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Propagation;
import org.springframework.transaction.annotation.Transactional;

import javax.persistence.criteria.CriteriaBuilder;
import javax.persistence.criteria.CriteriaQuery;
import javax.persistence.criteria.Predicate;
import javax.persistence.criteria.Root;
import java.util.List;

/**
 * A simple DTO Facility for MtrHistorialApagar.
 */
@Service
@Transactional(propagation = Propagation.REQUIRED, rollbackFor = Exception.class)
public class MtrHistorialApagarDeltaServiceImpl extends MtrHistorialApagarServiceImpl implements MtrHistorialApagarDeltaService {

    /**************************/
    /* Metodos Personalizados */
    /**************************/

    public List<MtrHistorialApagar> findAllOrderFechaHistorial() throws Exception {
        Sort sort = Sort.by(new Sort.Order(Sort.Direction.DESC, "fechaHistorial"));
        List<MtrHistorialApagar> mtrHistorialApagarList = this.mtrHistorialApagarDeltaRepository.findAll(sort) ;
        return mtrHistorialApagarList;
    }

    /***********************/
    /* Metodos de Busqueda */
    /***********************/

    protected Sort setFindAll(Sort sort) {
        return sort;
    }

    protected Sort setFind(MtrHistorialApagar req, ExampleMatcher matcher, Example<MtrHistorialApagar> example, Sort sort) {
        return sort;
    }

    protected void setFindPaginated(PageRequestByExample<MtrHistorialApagar> req, ExampleMatcher matcher, Example<MtrHistorialApagar> example) {
        return;
    }

    protected List<Predicate> setAdicionalDeltaPredicate(List<Predicate> predicates, MtrHistorialApagarResponse bean, CriteriaBuilder cb,
            CriteriaQuery<MtrHistorialApagar> query, Root<MtrHistorialApagar> root) throws Exception {

        MtrHistorialApagar entity = bean.getBean();
        //Ejemplo
        /*
        if (Optional.ofNullable(entity.get<VariableManytoOne>()).isPresent()) {
            Join<MtrHistorialApagar, <ClaseManytoOne>> from<ClaseManytoOne> = countRoot.join("<variableManytoOne>", JoinType.INNER);
        
            if (Optional.ofNullable(entity.get<VariableManytoOne>().get<Atributo>()).isPresent()) {
                Join<MtrHistorialApagar, <ClaseManytoOne>> from<ClaseManytoOne> = countRoot.join("<variableManytoOne>", JoinType.INNER);
                Predicate thirdCondition = cb.equal(from<ClaseManytoOne>.get("<Atributo>"), entity.get<ClaseManytoOne>().get<Atributo>());
                predicates.add(thirdCondition);
            }
            
        }
        query.orderBy(cb.desc(root.get("<campo entity>")));
        */
        return predicates;
    }

    protected Root<MtrHistorialApagar> setAdicionalDeltaTotalPredicate(MtrHistorialApagarResponse bean, Root<MtrHistorialApagar> countRoot) throws Exception {
        MtrHistorialApagar entity = bean.getBean();
        //Ejemplo
        /*
        if (Optional.ofNullable(entity.get<VariableManytoOne>()).isPresent()) {
            if (Optional.ofNullable(entity.get<VariableManytoOne>()).isPresent()) {
                Join<MtrHistorialApagar, <ClaseManytoOne>> from<ClaseManytoOne> = countRoot.join("<variableManytoOne>", JoinType.INNER);
            }
        }
        */
        return countRoot;
    }

    /****************/
    /* METODOS CRUD */
    /****************/

    protected String setValidacionesPrevias(MtrHistorialApagar bean) throws Exception {
        String mensaje = "";
        return mensaje;
    }

    protected MtrHistorialApagar setCreate(MtrHistorialApagar bean) throws Exception {
        return bean;
    }

    protected void setSave(MtrHistorialApagar dto) throws Exception {
        return;
    }

    protected void setDelete(Integer id) throws Exception {

    }

    protected void setDeleteAll() throws Exception {

    }

    /************************/
    /* METODOS CRUD Masivos */
    /************************/

    protected MtrHistorialApagar setUploadExcel(Cell currentCell, MtrHistorialApagar mtrHistorialApagar, int contador) throws Exception {
        mtrHistorialApagar = super.setUploadExcel(currentCell, mtrHistorialApagar, contador);
        return mtrHistorialApagar;
    }

    protected String setSaveMasivo(MtrHistorialApagar dto) throws Exception {
        return "";
    }

    protected List<MtrHistorialApagar> setBeforeDeleteMasivo(List<MtrHistorialApagar> listaDto) throws Exception {
        return listaDto;
    }

    protected void setAfterDeleteMasivo() throws Exception {
        return;
    }

    /*****************************/
    /* Metodos que generan Excel */
    /*****************************/

    protected void setDownloadExcelItem(MtrHistorialApagar bean, HSSFRow dataRow) {

    }

    protected void setDownloadExcel(HSSFSheet sheet) {

    }

    /*****************/
    /* Otros Metodos */
    /*****************/

}
