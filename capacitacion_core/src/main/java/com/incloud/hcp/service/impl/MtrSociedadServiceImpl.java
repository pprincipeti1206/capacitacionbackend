/*
 * Project home: https://github.com/jaxio/celerio-angular-quickstart
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Modificado por CARLOS BAZALAR
 * Email: cbazalarlarosa@gmail.com
 * Template pack-angular:src/main/java/dto/EntityDTOService.java.e.vm
 */
package com.incloud.hcp.service.impl;

import com.incloud.hcp.common.enums.AppParametriaLabelEnum;
import com.incloud.hcp.common.enums.AppParametriaModuloEnum;
import com.incloud.hcp.common.excel.ExcelDefault;
import com.incloud.hcp.domain.AppParametria;
import com.incloud.hcp.domain.MtrSociedad;
import com.incloud.hcp.domain.MtrSociedad_;
import com.incloud.hcp.domain.response.MtrSociedadResponse;
import com.incloud.hcp.exception.ServiceException;
import com.incloud.hcp.repository.delta.AppParametriaDeltaRepository;
import com.incloud.hcp.repository.delta.MtrSociedadDeltaRepository;
import com.incloud.hcp.service.MtrSociedadService;
import com.incloud.hcp.service._framework.bean.BeanCargaMasivoDTO;
import com.incloud.hcp.service._framework.bean.BeanListaMasivoDTO;
import com.incloud.hcp.service._framework.impl.JPACustomServiceImpl;
import com.incloud.hcp.service.requireNew.MtrSociedadRequireNewService;
import com.incloud.hcp.service.support.PageRequestByExample;
import com.incloud.hcp.utils.Constants;
import com.incloud.hcp.utils.PredicateUtils;
import org.apache.commons.beanutils.BeanUtils;
import org.apache.commons.lang.StringUtils;
import org.apache.poi.hssf.usermodel.HSSFRow;
import org.apache.poi.ss.usermodel.Cell;
import org.apache.poi.ss.usermodel.CellStyle;
import org.apache.poi.ss.usermodel.Row;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.i18n.LocaleContextHolder;
import org.springframework.data.domain.Example;
import org.springframework.data.domain.ExampleMatcher;
import org.springframework.data.domain.Sort;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Propagation;
import org.springframework.transaction.annotation.Transactional;

import javax.persistence.criteria.CriteriaBuilder;
import javax.persistence.criteria.Predicate;
import javax.persistence.criteria.Root;
import java.math.BigDecimal;
import java.util.ArrayList;
import java.util.List;
import java.util.Optional;

/**
 * A simple DTO Facility for MtrSociedad.
 */
@Service
@Transactional(propagation = Propagation.REQUIRED, rollbackFor = Exception.class)
public abstract class MtrSociedadServiceImpl extends JPACustomServiceImpl<MtrSociedadResponse, MtrSociedad, Integer> implements MtrSociedadService {

    protected final String NAME_SHEET = "MtrSociedad";
    protected final String CONFIG_TITLE = "com/incloud/hcp/excel/MtrSociedadExcel.xml";
    private final Integer REGISTROS_COLOR = 10;

    @Autowired
    protected AppParametriaDeltaRepository appParametriaDeltaRepository;

    @Autowired
    protected MtrSociedadDeltaRepository mtrSociedadDeltaRepository;

    @Autowired
    protected MtrSociedadRequireNewService mtrSociedadRequireNewService;

    /*****************************/
    /* Metodos de Busqueda       */
    /*****************************/

    protected final ExampleMatcher setAbstractFind(ExampleMatcher matcher) {
        matcher = ExampleMatcher.matching() //
                .withMatcher(MtrSociedad_.descBrv.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(MtrSociedad_.descripcion.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(MtrSociedad_.estado.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(MtrSociedad_.ruc.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(MtrSociedad_.usuarioClaveSol.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(MtrSociedad_.passwordClaveSol.getName(), match -> match.ignoreCase().startsWith());
        return matcher;
    }

    protected abstract Sort setFindAll(Sort sort);

    protected abstract Sort setFind(MtrSociedad req, ExampleMatcher matcher, Example<MtrSociedad> example, Sort sort);

    protected abstract void setFindPaginated(PageRequestByExample<MtrSociedad> req, ExampleMatcher matcher, Example<MtrSociedad> example);

    protected final MtrSociedad setObtenerBeanResponse(MtrSociedadResponse bean) {
        return bean.getBean();
    }

    protected final Class<MtrSociedad> setObtenerClassBean() {
        return MtrSociedad.class;
    }

    protected List<Predicate> setAbstractPredicate(MtrSociedadResponse bean, CriteriaBuilder cb, Root<MtrSociedad> root) {
        List<Predicate> predicates = new ArrayList<>();
        MtrSociedad entity = bean.getBean();
        /* Obtener condiciones */
        PredicateUtils.addPredicates(predicates, bean.getIdCondicion(), "id", entity.getId(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getDescBrvCondicion(), "descBrv", entity.getDescBrv(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getDescripcionCondicion(), "descripcion", entity.getDescripcion(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getEstadoCondicion(), "estado", entity.getEstado(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getRucCondicion(), "ruc", entity.getRuc(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getUsuarioClaveSolCondicion(), "usuarioClaveSol", entity.getUsuarioClaveSol(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getPasswordClaveSolCondicion(), "passwordClaveSol", entity.getPasswordClaveSol(), cb, root);
        /* Obtener valores de Lista */
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "id", bean.getIdList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "descBrv", bean.getDescBrvList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "descripcion", bean.getDescripcionList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "estado", bean.getEstadoList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "ruc", bean.getRucList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "usuarioClaveSol", bean.getUsuarioClaveSolList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "passwordClaveSol", bean.getPasswordClaveSolList(), cb, root);
        return predicates;
    }

    /****************/
    /* METODOS CRUD */
    /****************/

    protected final String validacionesPrevias(MtrSociedad bean) throws Exception {
        String mensaje = "";
        if (!Optional.of(bean.getDescBrv()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mtrSociedad.descBrv.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        if (!Optional.of(bean.getDescripcion()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mtrSociedad.descripcion.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        if (!Optional.of(bean.getEstado()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mtrSociedad.estado.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        String msg = this.setValidacionesPrevias(bean);
        if (StringUtils.isNotBlank(msg)) {
            mensaje += "* " + msg + "<br/>";
        }
        return mensaje;
    }

    protected String setValidacionesPrevias(MtrSociedad bean) throws Exception {
        String mensaje = "";
        return mensaje;
    }

    protected String validacionesPreviasCreate(MtrSociedad bean) throws Exception {
        String msg = null;
        MtrSociedad validar = null;
        validar = this.mtrSociedadDeltaRepository.getByDescripcion(bean.getDescripcion());
        if (Optional.ofNullable(validar).isPresent()) {
            msg = this.messageSource.getMessage("message.mtrSociedad.descripcion.duplicado", null, LocaleContextHolder.getLocale());
            return msg;
        }
        return msg;
    }

    protected String validacionesPreviasSave(MtrSociedad bean) throws Exception {
        String msg = null;
        MtrSociedad validar = null;
        validar = this.mtrSociedadDeltaRepository.getByDescripcion(bean.getDescripcion());
        if (Optional.ofNullable(validar).isPresent()) {
            if (bean.getId().intValue() != validar.getId().intValue()) {
                msg = this.messageSource.getMessage("message.mtrSociedad.descripcion.duplicado", null, LocaleContextHolder.getLocale());
                return msg;
            }
        }
        return msg;
    }

    protected MtrSociedad completarDatosBean(MtrSociedad bean) throws Exception {
        BigDecimal data = new BigDecimal(0.00);
        bean = this.setCompletarDatosBean(bean);
        return bean;
    }

    protected MtrSociedad setCompletarDatosBean(MtrSociedad bean) throws Exception {
        return bean;
    }

    protected final MtrSociedad setAbstractCreate(MtrSociedad dto) throws Exception {
        MtrSociedad bean = new MtrSociedad();
        bean = (MtrSociedad) BeanUtils.cloneBean(dto);
        return bean;
    }

    /************************/
    /* METODOS CRUD Masivos */
    /************************/

    protected MtrSociedad setUploadExcel(Cell currentCell, MtrSociedad mtrSociedad, int contador) throws Exception {
        Double valorDouble = new Double(0);
        BigDecimal valorDecimal = new BigDecimal(0);
        Boolean valorBoolean = new Boolean(true);
        String valorCadena = "";
        switch (contador) {
        case 1:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 100) {
                    throw new ServiceException("Valor Campo descBrv contiene mas de 100 caracter(es)");
                }
                mtrSociedad.setDescBrv(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo descBrv está en formato incorrecto");
            }
            break;
        case 2:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 200) {
                    throw new ServiceException("Valor Campo descripcion contiene mas de 200 caracter(es)");
                }
                mtrSociedad.setDescripcion(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo descripcion está en formato incorrecto");
            }
            break;
        case 3:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 1) {
                    throw new ServiceException("Valor Campo estado contiene mas de 1 caracter(es)");
                }
                mtrSociedad.setEstado(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo estado está en formato incorrecto");
            }
            break;
        case 4:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 255) {
                    throw new ServiceException("Valor Campo ruc contiene mas de 255 caracter(es)");
                }
                mtrSociedad.setRuc(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo ruc está en formato incorrecto");
            }
            break;
        case 5:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 100) {
                    throw new ServiceException("Valor Campo usuarioClaveSol contiene mas de 100 caracter(es)");
                }
                mtrSociedad.setUsuarioClaveSol(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo usuarioClaveSol está en formato incorrecto");
            }
            break;
        case 6:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 100) {
                    throw new ServiceException("Valor Campo passwordClaveSol contiene mas de 100 caracter(es)");
                }
                mtrSociedad.setPasswordClaveSol(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo passwordClaveSol está en formato incorrecto");
            }
            break;
        default:
            break;
        }
        return mtrSociedad;
    }

    protected AppParametria setObtenerRegistroConfiguracionUploadExcel() {
        AppParametria appParametriaData = this.appParametriaDeltaRepository.getByModuloAndLabelAndStatus(AppParametriaModuloEnum.CARGA_EXCEL.getEstado(),
                AppParametriaLabelEnum.INICIO_REGISTRO_DATA.getEstado(), Constants.ESTADO_ACTIVO);
        return appParametriaData;
    }

    protected MtrSociedad setInicializarBeanUpdateExcel() {
        MtrSociedad bean = new MtrSociedad();
        bean.setId(null);
        return bean;
    }

    protected final Integer setObtenerId(MtrSociedad bean) {
        return bean.getId();
    }

    /************************/
    /* Instancia de Bean    */
    /************************/

    protected final MtrSociedad createInstance() {
        MtrSociedad mtrSociedad = new MtrSociedad();
        return mtrSociedad;
    }

    protected final BeanCargaMasivoDTO<MtrSociedad> createInstanceMasivoDTO() {
        BeanCargaMasivoDTO<MtrSociedad> beanCargaMasivoDTO = new BeanCargaMasivoDTO<MtrSociedad>();
        return beanCargaMasivoDTO;

    }

    protected final BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrSociedad>> createInstanceListaMasivoDTO() {
        BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrSociedad>> beanCargaMasivoDTOBeanListaMasivoDTO = new BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrSociedad>>();
        return beanCargaMasivoDTOBeanListaMasivoDTO;
    }

    /*****************************/
    /* Metodos que generan Excel */
    /*****************************/

    protected String devuelveNombreSheet() {
        return this.NAME_SHEET;
    }

    protected String devuelveListaHeaderExcelXML() {
        return this.CONFIG_TITLE;
    }

    protected int setAbstractDownloadExcel(MtrSociedad bean, HSSFRow dataRow) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getDescBrv(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getDescripcion(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getEstado(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getRuc(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getUsuarioClaveSol(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getPasswordClaveSol(), dataRow.createCell(contador));
        contador++;
        return contador;
    }

    protected int setAbstractDownloadExcelSXLSX(MtrSociedad bean, Row dataRow, List<CellStyle> cellStyleList) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador), "I", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getDescBrv(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getDescripcion(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getEstado(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getRuc(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getUsuarioClaveSol(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getPasswordClaveSol(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        return contador;
    }

    protected String setAbstractGenerateInsertExcelSXLSX(MtrSociedad bean) {
        String fechaS = "";
        String sqlInsert = "INSERT INTO mtr_sociedad(";
        sqlInsert = sqlInsert + "mtr_sociedad_id" + ", ";
        sqlInsert = sqlInsert + "desc_brv" + ", ";
        sqlInsert = sqlInsert + "descripcion" + ", ";
        sqlInsert = sqlInsert + "estado" + ", ";
        sqlInsert = sqlInsert + "ruc" + ", ";
        sqlInsert = sqlInsert + "usuario_clave_sol" + ", ";
        sqlInsert = sqlInsert + "password_clave_sol" + ")";
        sqlInsert = sqlInsert + " VALUES (";
        sqlInsert = sqlInsert + bean.getId() + ", ";
        if (StringUtils.isBlank(bean.getDescBrv())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getDescBrv() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getDescripcion())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getDescripcion() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getEstado())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getEstado() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getRuc())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getRuc() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getUsuarioClaveSol())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getUsuarioClaveSol() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getPasswordClaveSol())) {
            sqlInsert = sqlInsert + "null";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getPasswordClaveSol() + "'";
        }
        sqlInsert = sqlInsert + " );";
        return sqlInsert;
    }

    /*****************/
    /* Otros Metodos */
    /*****************/

}
