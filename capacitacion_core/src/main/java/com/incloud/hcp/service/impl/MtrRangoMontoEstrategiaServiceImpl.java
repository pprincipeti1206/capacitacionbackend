/*
 * Project home: https://github.com/jaxio/celerio-angular-quickstart
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Modificado por CARLOS BAZALAR
 * Email: cbazalarlarosa@gmail.com
 * Template pack-angular:src/main/java/dto/EntityDTOService.java.e.vm
 */
package com.incloud.hcp.service.impl;

import com.incloud.hcp.common.enums.AppParametriaLabelEnum;
import com.incloud.hcp.common.enums.AppParametriaModuloEnum;
import com.incloud.hcp.common.excel.ExcelDefault;
import com.incloud.hcp.common.graph.GraphBarChart;
import com.incloud.hcp.common.graph.GraphBean;
import com.incloud.hcp.common.graph.GraphDataset;
import com.incloud.hcp.common.graph.GraphPieChart;
import com.incloud.hcp.domain.*;
import com.incloud.hcp.domain.response.MtrRangoMontoEstrategiaResponse;
import com.incloud.hcp.exception.ServiceException;
import com.incloud.hcp.repository.delta.AppParametriaDeltaRepository;
import com.incloud.hcp.repository.delta.MtrMonedaDeltaRepository;
import com.incloud.hcp.repository.delta.MtrRangoMontoEstrategiaDeltaRepository;
import com.incloud.hcp.repository.delta.MtrTipoContratoDeltaRepository;
import com.incloud.hcp.service.MtrRangoMontoEstrategiaService;
import com.incloud.hcp.service._framework.bean.BeanCargaMasivoDTO;
import com.incloud.hcp.service._framework.bean.BeanListaMasivoDTO;
import com.incloud.hcp.service._framework.impl.JPACustomServiceImpl;
import com.incloud.hcp.service.delta.MtrMonedaDeltaService;
import com.incloud.hcp.service.delta.MtrTipoContratoDeltaService;
import com.incloud.hcp.service.requireNew.MtrRangoMontoEstrategiaRequireNewService;
import com.incloud.hcp.service.support.PageRequestByExample;
import com.incloud.hcp.utils.Constants;
import com.incloud.hcp.utils.PredicateUtils;
import org.apache.commons.beanutils.BeanUtils;
import org.apache.commons.lang.StringUtils;
import org.apache.poi.hssf.usermodel.HSSFRow;
import org.apache.poi.ss.usermodel.Cell;
import org.apache.poi.ss.usermodel.CellStyle;
import org.apache.poi.ss.usermodel.Row;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.i18n.LocaleContextHolder;
import org.springframework.data.domain.Example;
import org.springframework.data.domain.ExampleMatcher;
import org.springframework.data.domain.Sort;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Propagation;
import org.springframework.transaction.annotation.Transactional;

import javax.persistence.criteria.CriteriaBuilder;
import javax.persistence.criteria.Predicate;
import javax.persistence.criteria.Root;
import java.math.BigDecimal;
import java.util.ArrayList;
import java.util.List;
import java.util.Optional;

/**
 * A simple DTO Facility for MtrRangoMontoEstrategia.
 */
@Service
@Transactional(propagation = Propagation.REQUIRED, rollbackFor = Exception.class)
public abstract class MtrRangoMontoEstrategiaServiceImpl extends JPACustomServiceImpl<MtrRangoMontoEstrategiaResponse, MtrRangoMontoEstrategia, Integer>
        implements MtrRangoMontoEstrategiaService {

    protected final String NAME_SHEET = "MtrRangoMontoEstrategia";
    protected final String CONFIG_TITLE = "com/incloud/hcp/excel/MtrRangoMontoEstrategiaExcel.xml";
    private final Integer REGISTROS_COLOR = 10;

    @Autowired
    protected AppParametriaDeltaRepository appParametriaDeltaRepository;

    @Autowired
    protected MtrRangoMontoEstrategiaDeltaRepository mtrRangoMontoEstrategiaDeltaRepository;

    @Autowired
    protected MtrRangoMontoEstrategiaRequireNewService mtrRangoMontoEstrategiaRequireNewService;

    @Autowired
    protected MtrMonedaDeltaService mtrMonedaDeltaService;

    @Autowired
    protected MtrMonedaDeltaRepository mtrMonedaDeltaRepository;

    @Autowired
    protected MtrTipoContratoDeltaService mtrTipoContratoDeltaService;

    @Autowired
    protected MtrTipoContratoDeltaRepository mtrTipoContratoDeltaRepository;

    /*****************************/
    /* Metodos de Busqueda       */
    /*****************************/

    protected final ExampleMatcher setAbstractFind(ExampleMatcher matcher) {
        matcher = ExampleMatcher.matching() //
                .withMatcher(MtrRangoMontoEstrategia_.descripcion.getName(), match -> match.ignoreCase().startsWith());
        return matcher;
    }

    protected abstract Sort setFindAll(Sort sort);

    protected abstract Sort setFind(MtrRangoMontoEstrategia req, ExampleMatcher matcher, Example<MtrRangoMontoEstrategia> example, Sort sort);

    protected abstract void setFindPaginated(PageRequestByExample<MtrRangoMontoEstrategia> req, ExampleMatcher matcher,
            Example<MtrRangoMontoEstrategia> example);

    protected final MtrRangoMontoEstrategia setObtenerBeanResponse(MtrRangoMontoEstrategiaResponse bean) {
        return bean.getBean();
    }

    protected final Class<MtrRangoMontoEstrategia> setObtenerClassBean() {
        return MtrRangoMontoEstrategia.class;
    }

    protected List<Predicate> setAbstractPredicate(MtrRangoMontoEstrategiaResponse bean, CriteriaBuilder cb, Root<MtrRangoMontoEstrategia> root) {
        List<Predicate> predicates = new ArrayList<>();
        MtrRangoMontoEstrategia entity = bean.getBean();
        /* Obtener condiciones */
        PredicateUtils.addPredicates(predicates, bean.getIdCondicion(), "id", entity.getId(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getDescripcionCondicion(), "descripcion", entity.getDescripcion(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getValorMinimoCondicion(), "valorMinimo", entity.getValorMinimo(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getValorMaximoCondicion(), "valorMaximo", entity.getValorMaximo(), cb, root);
        //PredicateUtils.addPredicates(predicates, bean.getMtrMonedaCondicion(), "mtrMoneda", entity.getMtrMoneda(), cb, root);
        //PredicateUtils.addPredicates(predicates, bean.getMtrTipoContratoCondicion(), "mtrTipoContrato", entity.getMtrTipoContrato(), cb, root);
        /* Obtener valores de Lista */
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "id", bean.getIdList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "descripcion", bean.getDescripcionList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "valorMinimo", bean.getValorMinimoList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "valorMaximo", bean.getValorMaximoList(), cb, root);
        //PredicateUtils.addPredicatesListValorBean(predicates, "mtrMoneda", bean.getMtrMonedaList(), cb, root);
        //PredicateUtils.addPredicatesListValorBean(predicates, "mtrTipoContrato", bean.getMtrTipoContratoList(), cb, root);
        return predicates;
    }

    /****************/
    /* METODOS CRUD */
    /****************/

    protected final String validacionesPrevias(MtrRangoMontoEstrategia bean) throws Exception {
        String mensaje = "";
        if (!Optional.of(bean.getDescripcion()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mtrRangoMontoEstrategia.descripcion.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        if (!Optional.of(bean.getValorMinimo()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mtrRangoMontoEstrategia.valorMinimo.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        String msg = this.setValidacionesPrevias(bean);
        if (StringUtils.isNotBlank(msg)) {
            mensaje += "* " + msg + "<br/>";
        }
        return mensaje;
    }

    protected String setValidacionesPrevias(MtrRangoMontoEstrategia bean) throws Exception {
        String mensaje = "";
        return mensaje;
    }

    protected String validacionesPreviasCreate(MtrRangoMontoEstrategia bean) throws Exception {
        String msg = null;
        MtrRangoMontoEstrategia validar = null;
        validar = this.mtrRangoMontoEstrategiaDeltaRepository.getByDescripcion(bean.getDescripcion());
        if (Optional.ofNullable(validar).isPresent()) {
            msg = this.messageSource.getMessage("message.mtrRangoMontoEstrategia.descripcion.duplicado", null, LocaleContextHolder.getLocale());
            return msg;
        }
        return msg;
    }

    protected String validacionesPreviasSave(MtrRangoMontoEstrategia bean) throws Exception {
        String msg = null;
        MtrRangoMontoEstrategia validar = null;
        validar = this.mtrRangoMontoEstrategiaDeltaRepository.getByDescripcion(bean.getDescripcion());
        if (Optional.ofNullable(validar).isPresent()) {
            if (bean.getId().intValue() != validar.getId().intValue()) {
                msg = this.messageSource.getMessage("message.mtrRangoMontoEstrategia.descripcion.duplicado", null, LocaleContextHolder.getLocale());
                return msg;
            }
        }
        return msg;
    }

    protected MtrRangoMontoEstrategia completarDatosBean(MtrRangoMontoEstrategia bean) throws Exception {
        BigDecimal data = new BigDecimal(0.00);
        bean = this.setCompletarDatosBean(bean);
        return bean;
    }

    protected MtrRangoMontoEstrategia setCompletarDatosBean(MtrRangoMontoEstrategia bean) throws Exception {
        return bean;
    }

    protected final MtrRangoMontoEstrategia setAbstractCreate(MtrRangoMontoEstrategia dto) throws Exception {
        MtrRangoMontoEstrategia bean = new MtrRangoMontoEstrategia();
        bean = (MtrRangoMontoEstrategia) BeanUtils.cloneBean(dto);
        return bean;
    }

    /************************/
    /* METODOS CRUD Masivos */
    /************************/

    protected MtrRangoMontoEstrategia setUploadExcel(Cell currentCell, MtrRangoMontoEstrategia mtrRangoMontoEstrategia, int contador) throws Exception {
        Double valorDouble = new Double(0);
        BigDecimal valorDecimal = new BigDecimal(0);
        Boolean valorBoolean = new Boolean(true);
        String valorCadena = "";
        switch (contador) {
        case 1:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 60) {
                    throw new ServiceException("Valor Campo descripcion contiene mas de 60 caracter(es)");
                }
                mtrRangoMontoEstrategia.setDescripcion(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo descripcion está en formato incorrecto");
            }
            break;
        case 2:
            try {
                valorDouble = new Double(currentCell.getNumericCellValue());
                mtrRangoMontoEstrategia.setValorMinimo(valorDouble.intValue());
            } catch (Exception e) {
                throw new ServiceException("Valor Campo valorMinimo está en formato incorrecto");
            }
            break;
        case 3:
            try {
                valorDouble = new Double(currentCell.getNumericCellValue());
                mtrRangoMontoEstrategia.setValorMaximo(valorDouble.intValue());
            } catch (Exception e) {
                throw new ServiceException("Valor Campo valorMaximo está en formato incorrecto");
            }
            break;
        default:
            break;
        }
        return mtrRangoMontoEstrategia;
    }

    protected AppParametria setObtenerRegistroConfiguracionUploadExcel() {
        AppParametria appParametriaData = this.appParametriaDeltaRepository.getByModuloAndLabelAndStatus(AppParametriaModuloEnum.CARGA_EXCEL.getEstado(),
                AppParametriaLabelEnum.INICIO_REGISTRO_DATA.getEstado(), Constants.ESTADO_ACTIVO);
        return appParametriaData;
    }

    protected MtrRangoMontoEstrategia setInicializarBeanUpdateExcel() {
        MtrRangoMontoEstrategia bean = new MtrRangoMontoEstrategia();
        bean.setId(null);
        return bean;
    }

    protected final Integer setObtenerId(MtrRangoMontoEstrategia bean) {
        return bean.getId();
    }

    /************************/
    /* Instancia de Bean    */
    /************************/

    protected final MtrRangoMontoEstrategia createInstance() {
        MtrRangoMontoEstrategia mtrRangoMontoEstrategia = new MtrRangoMontoEstrategia();
        return mtrRangoMontoEstrategia;
    }

    protected final BeanCargaMasivoDTO<MtrRangoMontoEstrategia> createInstanceMasivoDTO() {
        BeanCargaMasivoDTO<MtrRangoMontoEstrategia> beanCargaMasivoDTO = new BeanCargaMasivoDTO<MtrRangoMontoEstrategia>();
        return beanCargaMasivoDTO;

    }

    protected final BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrRangoMontoEstrategia>> createInstanceListaMasivoDTO() {
        BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrRangoMontoEstrategia>> beanCargaMasivoDTOBeanListaMasivoDTO = new BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrRangoMontoEstrategia>>();
        return beanCargaMasivoDTOBeanListaMasivoDTO;
    }

    /*****************************/
    /* Metodos que generan Excel */
    /*****************************/

    protected String devuelveNombreSheet() {
        return this.NAME_SHEET;
    }

    protected String devuelveListaHeaderExcelXML() {
        return this.CONFIG_TITLE;
    }

    protected int setAbstractDownloadExcel(MtrRangoMontoEstrategia bean, HSSFRow dataRow) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getDescripcion(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getValorMinimo(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getValorMaximo(), dataRow.createCell(contador));
        contador++;
        return contador;
    }

    protected int setAbstractDownloadExcelSXLSX(MtrRangoMontoEstrategia bean, Row dataRow, List<CellStyle> cellStyleList) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador), "I", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getDescripcion(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getValorMinimo(), dataRow.createCell(contador), "I", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getValorMaximo(), dataRow.createCell(contador), "I", cellStyleList);
        contador++;
        return contador;
    }

    protected String setAbstractGenerateInsertExcelSXLSX(MtrRangoMontoEstrategia bean) {
        String fechaS = "";
        String sqlInsert = "INSERT INTO mtr_rango_monto_estrategia(";
        sqlInsert = sqlInsert + "mtr_rango_monto_estrategia_id" + ", ";
        sqlInsert = sqlInsert + "descripcion" + ", ";
        sqlInsert = sqlInsert + "valor_minimo" + ", ";
        sqlInsert = sqlInsert + "valor_maximo" + ", ";
        sqlInsert = sqlInsert + "mtr_moneda_id" + ", ";
        sqlInsert = sqlInsert + "mtr_tipo_contrato_id" + ")";
        sqlInsert = sqlInsert + " VALUES (";
        sqlInsert = sqlInsert + bean.getId() + ", ";
        if (StringUtils.isBlank(bean.getDescripcion())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getDescripcion() + "'" + ", ";
        }
        sqlInsert = sqlInsert + bean.getValorMinimo() + ", ";
        sqlInsert = sqlInsert + bean.getValorMaximo() + ", ";
        sqlInsert = sqlInsert + bean.getMtrMoneda().getId() + ", ";
        sqlInsert = sqlInsert + bean.getMtrTipoContrato().getId();
        sqlInsert = sqlInsert + " );";
        return sqlInsert;
    }

    /*****************/
    /* Otros Metodos */
    /*****************/

    public Long countByMtrMoneda(MtrMoneda mtrMoneda) {
        return this.mtrRangoMontoEstrategiaDeltaRepository.countByMtrMoneda(mtrMoneda);
    }

    public GraphBean graphByMtrMoneda() {
        List<MtrMoneda> lista = this.mtrMonedaDeltaRepository.findAll();
        if (lista == null || lista.size() <= 0) {
            return null;
        }
        GraphBean graphBean = new GraphBean();

        /* Obteniendo Pie Chart */
        GraphPieChart graphPieChart = new GraphPieChart();
        GraphDataset graphDataset = new GraphDataset();
        GraphDataset[] listaGraphDataset = new GraphDataset[1];
        Long[] data = new Long[lista.size()];
        String[] labels = new String[lista.size()];
        String[] backgroundColor = new String[lista.size()];
        String[] hoverBackgroundColor = new String[lista.size()];
        String[] borderColor = new String[lista.size()];
        String[] hoverBorderColor = new String[lista.size()];
        int contador = 0;
        int contadorColor = 0;
        for (MtrMoneda bean : lista) {
            Long valor = this.mtrRangoMontoEstrategiaDeltaRepository.countByMtrMoneda(bean);
            String descripcion = this.setGraphDescripcionByMtrMoneda(bean);
            data[contador] = valor;
            labels[contador] = descripcion;
            backgroundColor[contador] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBackgroundColor[contador] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            borderColor[contador] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBorderColor[contador] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            contador++;
            contadorColor++;
            if (contadorColor >= REGISTROS_COLOR) {
                contadorColor = 1;
            }
        }
        String titulo = this.setGraphPieChartTituloByMtrMoneda();
        graphDataset.setLabel(titulo);
        graphDataset.setData(data);
        graphDataset.setBackgroundColor(backgroundColor);
        graphDataset.setHoverBackgroundColor(hoverBackgroundColor);
        listaGraphDataset[0] = graphDataset;
        graphPieChart.setDatasets(listaGraphDataset);
        graphPieChart.setLabels(labels);
        graphBean.setPiechart(graphPieChart);

        GraphBarChart graphBarChart = new GraphBarChart();
        GraphDataset[] listaGraphDatasetBar = new GraphDataset[lista.size()];
        String[] labelsBar = new String[1];
        labelsBar[0] = this.setGraphBarChartTituloByMtrMoneda();
        contador = 0;
        contadorColor = 0;
        for (MtrMoneda bean : lista) {
            Long valor = this.mtrRangoMontoEstrategiaDeltaRepository.countByMtrMoneda(bean);
            String descripcion = this.setGraphDescripcionByMtrMoneda(bean);
            GraphDataset graphDatasetBar = new GraphDataset();
            Long[] dataBar = new Long[1];

            String[] backgroundColorBar = new String[1];
            String[] hoverBackgroundColorBar = new String[1];
            String[] borderColorBar = new String[1];
            String[] hoverBorderColorBar = new String[1];

            dataBar[0] = valor;
            backgroundColorBar[0] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBackgroundColorBar[0] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            borderColorBar[0] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBorderColorBar[0] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];

            graphDatasetBar.setLabel(descripcion);
            graphDatasetBar.setData(dataBar);
            graphDatasetBar.setBackgroundColor(backgroundColorBar);
            graphDatasetBar.setHoverBackgroundColor(hoverBackgroundColorBar);
            graphDatasetBar.setBorderColor(borderColorBar);
            graphDatasetBar.setHoverBorderColor(hoverBorderColorBar);
            listaGraphDatasetBar[contador] = graphDatasetBar;

            contador++;
            contadorColor++;
            if (contadorColor >= REGISTROS_COLOR) {
                contadorColor = 1;
            }

        }
        graphBarChart.setDatasets(listaGraphDatasetBar);
        graphBarChart.setLabels(labelsBar);
        graphBean.setBarchart(graphBarChart);
        return graphBean;
    }

    protected String setGraphDescripcionByMtrMoneda(MtrMoneda mtrMoneda) {
        return mtrMoneda.getId().toString();
    }

    protected String setGraphPieChartTituloByMtrMoneda() {
        return "MtrMoneda";
    }

    protected String setGraphBarChartTituloByMtrMoneda() {
        return "MtrMoneda";
    }

    public Long countByMtrTipoContrato(MtrTipoContrato mtrTipoContrato) {
        return this.mtrRangoMontoEstrategiaDeltaRepository.countByMtrTipoContrato(mtrTipoContrato);
    }

    public GraphBean graphByMtrTipoContrato() {
        List<MtrTipoContrato> lista = this.mtrTipoContratoDeltaRepository.findAll();
        if (lista == null || lista.size() <= 0) {
            return null;
        }
        GraphBean graphBean = new GraphBean();

        /* Obteniendo Pie Chart */
        GraphPieChart graphPieChart = new GraphPieChart();
        GraphDataset graphDataset = new GraphDataset();
        GraphDataset[] listaGraphDataset = new GraphDataset[1];
        Long[] data = new Long[lista.size()];
        String[] labels = new String[lista.size()];
        String[] backgroundColor = new String[lista.size()];
        String[] hoverBackgroundColor = new String[lista.size()];
        String[] borderColor = new String[lista.size()];
        String[] hoverBorderColor = new String[lista.size()];
        int contador = 0;
        int contadorColor = 0;
        for (MtrTipoContrato bean : lista) {
            Long valor = this.mtrRangoMontoEstrategiaDeltaRepository.countByMtrTipoContrato(bean);
            String descripcion = this.setGraphDescripcionByMtrTipoContrato(bean);
            data[contador] = valor;
            labels[contador] = descripcion;
            backgroundColor[contador] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBackgroundColor[contador] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            borderColor[contador] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBorderColor[contador] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            contador++;
            contadorColor++;
            if (contadorColor >= REGISTROS_COLOR) {
                contadorColor = 1;
            }
        }
        String titulo = this.setGraphPieChartTituloByMtrTipoContrato();
        graphDataset.setLabel(titulo);
        graphDataset.setData(data);
        graphDataset.setBackgroundColor(backgroundColor);
        graphDataset.setHoverBackgroundColor(hoverBackgroundColor);
        listaGraphDataset[0] = graphDataset;
        graphPieChart.setDatasets(listaGraphDataset);
        graphPieChart.setLabels(labels);
        graphBean.setPiechart(graphPieChart);

        GraphBarChart graphBarChart = new GraphBarChart();
        GraphDataset[] listaGraphDatasetBar = new GraphDataset[lista.size()];
        String[] labelsBar = new String[1];
        labelsBar[0] = this.setGraphBarChartTituloByMtrTipoContrato();
        contador = 0;
        contadorColor = 0;
        for (MtrTipoContrato bean : lista) {
            Long valor = this.mtrRangoMontoEstrategiaDeltaRepository.countByMtrTipoContrato(bean);
            String descripcion = this.setGraphDescripcionByMtrTipoContrato(bean);
            GraphDataset graphDatasetBar = new GraphDataset();
            Long[] dataBar = new Long[1];

            String[] backgroundColorBar = new String[1];
            String[] hoverBackgroundColorBar = new String[1];
            String[] borderColorBar = new String[1];
            String[] hoverBorderColorBar = new String[1];

            dataBar[0] = valor;
            backgroundColorBar[0] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBackgroundColorBar[0] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            borderColorBar[0] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBorderColorBar[0] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];

            graphDatasetBar.setLabel(descripcion);
            graphDatasetBar.setData(dataBar);
            graphDatasetBar.setBackgroundColor(backgroundColorBar);
            graphDatasetBar.setHoverBackgroundColor(hoverBackgroundColorBar);
            graphDatasetBar.setBorderColor(borderColorBar);
            graphDatasetBar.setHoverBorderColor(hoverBorderColorBar);
            listaGraphDatasetBar[contador] = graphDatasetBar;

            contador++;
            contadorColor++;
            if (contadorColor >= REGISTROS_COLOR) {
                contadorColor = 1;
            }

        }
        graphBarChart.setDatasets(listaGraphDatasetBar);
        graphBarChart.setLabels(labelsBar);
        graphBean.setBarchart(graphBarChart);
        return graphBean;
    }

    protected String setGraphDescripcionByMtrTipoContrato(MtrTipoContrato mtrTipoContrato) {
        return mtrTipoContrato.getId().toString();
    }

    protected String setGraphPieChartTituloByMtrTipoContrato() {
        return "MtrTipoContrato";
    }

    protected String setGraphBarChartTituloByMtrTipoContrato() {
        return "MtrTipoContrato";
    }

}
