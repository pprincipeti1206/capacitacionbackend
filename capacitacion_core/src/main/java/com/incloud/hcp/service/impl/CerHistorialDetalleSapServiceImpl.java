/*
 * Project home: https://github.com/jaxio/celerio-angular-quickstart
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Modificado por CARLOS BAZALAR
 * Email: cbazalarlarosa@gmail.com
 * Template pack-angular:src/main/java/dto/EntityDTOService.java.e.vm
 */
package com.incloud.hcp.service.impl;

import com.incloud.hcp.common.enums.AppParametriaLabelEnum;
import com.incloud.hcp.common.enums.AppParametriaModuloEnum;
import com.incloud.hcp.common.excel.ExcelDefault;
import com.incloud.hcp.common.graph.GraphBarChart;
import com.incloud.hcp.common.graph.GraphBean;
import com.incloud.hcp.common.graph.GraphDataset;
import com.incloud.hcp.common.graph.GraphPieChart;
import com.incloud.hcp.domain.AppParametria;
import com.incloud.hcp.domain.CerHistorial;
import com.incloud.hcp.domain.CerHistorialDetalleSap;
import com.incloud.hcp.domain.CerHistorialDetalleSap_;
import com.incloud.hcp.domain.response.CerHistorialDetalleSapResponse;
import com.incloud.hcp.exception.ServiceException;
import com.incloud.hcp.repository.delta.AppParametriaDeltaRepository;
import com.incloud.hcp.repository.delta.CerHistorialDeltaRepository;
import com.incloud.hcp.repository.delta.CerHistorialDetalleSapDeltaRepository;
import com.incloud.hcp.service.CerHistorialDetalleSapService;
import com.incloud.hcp.service._framework.bean.BeanCargaMasivoDTO;
import com.incloud.hcp.service._framework.bean.BeanListaMasivoDTO;
import com.incloud.hcp.service._framework.impl.JPACustomServiceImpl;
import com.incloud.hcp.service.delta.CerHistorialDeltaService;
import com.incloud.hcp.service.requireNew.CerHistorialDetalleSapRequireNewService;
import com.incloud.hcp.service.support.PageRequestByExample;
import com.incloud.hcp.utils.Constants;
import com.incloud.hcp.utils.PredicateUtils;
import org.apache.commons.beanutils.BeanUtils;
import org.apache.commons.lang.StringUtils;
import org.apache.poi.hssf.usermodel.HSSFRow;
import org.apache.poi.ss.usermodel.Cell;
import org.apache.poi.ss.usermodel.CellStyle;
import org.apache.poi.ss.usermodel.Row;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.i18n.LocaleContextHolder;
import org.springframework.data.domain.Example;
import org.springframework.data.domain.ExampleMatcher;
import org.springframework.data.domain.Sort;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Propagation;
import org.springframework.transaction.annotation.Transactional;

import javax.persistence.criteria.CriteriaBuilder;
import javax.persistence.criteria.Predicate;
import javax.persistence.criteria.Root;
import java.math.BigDecimal;
import java.util.ArrayList;
import java.util.List;
import java.util.Optional;

/**
 * A simple DTO Facility for CerHistorialDetalleSap.
 */
@Service
@Transactional(propagation = Propagation.REQUIRED, rollbackFor = Exception.class)
public abstract class CerHistorialDetalleSapServiceImpl extends JPACustomServiceImpl<CerHistorialDetalleSapResponse, CerHistorialDetalleSap, Integer>
        implements CerHistorialDetalleSapService {

    protected final String NAME_SHEET = "CerHistorialDetalleSap";
    protected final String CONFIG_TITLE = "com/incloud/hcp/excel/CerHistorialDetalleSapExcel.xml";
    private final Integer REGISTROS_COLOR = 10;

    @Autowired
    protected AppParametriaDeltaRepository appParametriaDeltaRepository;

    @Autowired
    protected CerHistorialDetalleSapDeltaRepository cerHistorialDetalleSapDeltaRepository;

    @Autowired
    protected CerHistorialDetalleSapRequireNewService cerHistorialDetalleSapRequireNewService;

    @Autowired
    protected CerHistorialDeltaService cerHistorialDeltaService;

    @Autowired
    protected CerHistorialDeltaRepository cerHistorialDeltaRepository;

    /*****************************/
    /* Metodos de Busqueda       */
    /*****************************/

    protected final ExampleMatcher setAbstractFind(ExampleMatcher matcher) {
        matcher = ExampleMatcher.matching() //
                .withMatcher(CerHistorialDetalleSap_.descripcion.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(CerHistorialDetalleSap_.posicionSap.getName(), match -> match.ignoreCase().startsWith());
        return matcher;
    }

    protected abstract Sort setFindAll(Sort sort);

    protected abstract Sort setFind(CerHistorialDetalleSap req, ExampleMatcher matcher, Example<CerHistorialDetalleSap> example, Sort sort);

    protected abstract void setFindPaginated(PageRequestByExample<CerHistorialDetalleSap> req, ExampleMatcher matcher, Example<CerHistorialDetalleSap> example);

    protected final CerHistorialDetalleSap setObtenerBeanResponse(CerHistorialDetalleSapResponse bean) {
        return bean.getBean();
    }

    protected final Class<CerHistorialDetalleSap> setObtenerClassBean() {
        return CerHistorialDetalleSap.class;
    }

    protected List<Predicate> setAbstractPredicate(CerHistorialDetalleSapResponse bean, CriteriaBuilder cb, Root<CerHistorialDetalleSap> root) {
        List<Predicate> predicates = new ArrayList<>();
        CerHistorialDetalleSap entity = bean.getBean();
        /* Obtener condiciones */
        PredicateUtils.addPredicates(predicates, bean.getIdCondicion(), "id", entity.getId(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getDescripcionCondicion(), "descripcion", entity.getDescripcion(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getPosicionSapCondicion(), "posicionSap", entity.getPosicionSap(), cb, root);
        //PredicateUtils.addPredicates(predicates, bean.getCerHistorialCondicion(), "cerHistorial", entity.getCerHistorial(), cb, root);
        /* Obtener valores de Lista */
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "id", bean.getIdList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "descripcion", bean.getDescripcionList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "posicionSap", bean.getPosicionSapList(), cb, root);
        //PredicateUtils.addPredicatesListValorBean(predicates, "cerHistorial", bean.getCerHistorialList(), cb, root);
        return predicates;
    }

    /****************/
    /* METODOS CRUD */
    /****************/

    protected final String validacionesPrevias(CerHistorialDetalleSap bean) throws Exception {
        String mensaje = "";
        if (!Optional.of(bean.getDescripcion()).isPresent()) {
            String msg = this.messageSource.getMessage("message.cerHistorialDetalleSap.descripcion.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        String msg = this.setValidacionesPrevias(bean);
        if (StringUtils.isNotBlank(msg)) {
            mensaje += "* " + msg + "<br/>";
        }
        return mensaje;
    }

    protected String setValidacionesPrevias(CerHistorialDetalleSap bean) throws Exception {
        String mensaje = "";
        return mensaje;
    }

    protected String validacionesPreviasCreate(CerHistorialDetalleSap bean) throws Exception {
        String msg = null;
        CerHistorialDetalleSap validar = null;
        return msg;
    }

    protected String validacionesPreviasSave(CerHistorialDetalleSap bean) throws Exception {
        String msg = null;
        CerHistorialDetalleSap validar = null;
        return msg;
    }

    protected CerHistorialDetalleSap completarDatosBean(CerHistorialDetalleSap bean) throws Exception {
        BigDecimal data = new BigDecimal(0.00);
        bean = this.setCompletarDatosBean(bean);
        return bean;
    }

    protected CerHistorialDetalleSap setCompletarDatosBean(CerHistorialDetalleSap bean) throws Exception {
        return bean;
    }

    protected final CerHistorialDetalleSap setAbstractCreate(CerHistorialDetalleSap dto) throws Exception {
        CerHistorialDetalleSap bean = new CerHistorialDetalleSap();
        bean = (CerHistorialDetalleSap) BeanUtils.cloneBean(dto);
        return bean;
    }

    /************************/
    /* METODOS CRUD Masivos */
    /************************/

    protected CerHistorialDetalleSap setUploadExcel(Cell currentCell, CerHistorialDetalleSap cerHistorialDetalleSap, int contador) throws Exception {
        Double valorDouble = new Double(0);
        BigDecimal valorDecimal = new BigDecimal(0);
        Boolean valorBoolean = new Boolean(true);
        String valorCadena = "";
        switch (contador) {
        case 1:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 4000) {
                    throw new ServiceException("Valor Campo descripcion contiene mas de 4000 caracter(es)");
                }
                cerHistorialDetalleSap.setDescripcion(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo descripcion está en formato incorrecto");
            }
            break;
        case 2:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 20) {
                    throw new ServiceException("Valor Campo posicionSap contiene mas de 20 caracter(es)");
                }
                cerHistorialDetalleSap.setPosicionSap(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo posicionSap está en formato incorrecto");
            }
            break;
        default:
            break;
        }
        return cerHistorialDetalleSap;
    }

    protected AppParametria setObtenerRegistroConfiguracionUploadExcel() {
        AppParametria appParametriaData = this.appParametriaDeltaRepository.getByModuloAndLabelAndStatus(AppParametriaModuloEnum.CARGA_EXCEL.getEstado(),
                AppParametriaLabelEnum.INICIO_REGISTRO_DATA.getEstado(), Constants.ESTADO_ACTIVO);
        return appParametriaData;
    }

    protected CerHistorialDetalleSap setInicializarBeanUpdateExcel() {
        CerHistorialDetalleSap bean = new CerHistorialDetalleSap();
        bean.setId(null);
        return bean;
    }

    protected final Integer setObtenerId(CerHistorialDetalleSap bean) {
        return bean.getId();
    }

    /************************/
    /* Instancia de Bean    */
    /************************/

    protected final CerHistorialDetalleSap createInstance() {
        CerHistorialDetalleSap cerHistorialDetalleSap = new CerHistorialDetalleSap();
        return cerHistorialDetalleSap;
    }

    protected final BeanCargaMasivoDTO<CerHistorialDetalleSap> createInstanceMasivoDTO() {
        BeanCargaMasivoDTO<CerHistorialDetalleSap> beanCargaMasivoDTO = new BeanCargaMasivoDTO<CerHistorialDetalleSap>();
        return beanCargaMasivoDTO;

    }

    protected final BeanListaMasivoDTO<BeanCargaMasivoDTO<CerHistorialDetalleSap>> createInstanceListaMasivoDTO() {
        BeanListaMasivoDTO<BeanCargaMasivoDTO<CerHistorialDetalleSap>> beanCargaMasivoDTOBeanListaMasivoDTO = new BeanListaMasivoDTO<BeanCargaMasivoDTO<CerHistorialDetalleSap>>();
        return beanCargaMasivoDTOBeanListaMasivoDTO;
    }

    /*****************************/
    /* Metodos que generan Excel */
    /*****************************/

    protected String devuelveNombreSheet() {
        return this.NAME_SHEET;
    }

    protected String devuelveListaHeaderExcelXML() {
        return this.CONFIG_TITLE;
    }

    protected int setAbstractDownloadExcel(CerHistorialDetalleSap bean, HSSFRow dataRow) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getDescripcion(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getPosicionSap(), dataRow.createCell(contador));
        contador++;
        return contador;
    }

    protected int setAbstractDownloadExcelSXLSX(CerHistorialDetalleSap bean, Row dataRow, List<CellStyle> cellStyleList) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador), "I", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getDescripcion(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getPosicionSap(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        return contador;
    }

    protected String setAbstractGenerateInsertExcelSXLSX(CerHistorialDetalleSap bean) {
        String fechaS = "";
        String sqlInsert = "INSERT INTO cer_historial_detalle_sap(";
        sqlInsert = sqlInsert + "cer_historial_detalle_sap_id" + ", ";
        sqlInsert = sqlInsert + "descripcion" + ", ";
        sqlInsert = sqlInsert + "posicion_sap" + ", ";
        sqlInsert = sqlInsert + "cer_historial_id" + ")";
        sqlInsert = sqlInsert + " VALUES (";
        sqlInsert = sqlInsert + bean.getId() + ", ";
        if (StringUtils.isBlank(bean.getDescripcion())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getDescripcion() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getPosicionSap())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getPosicionSap() + "'" + ", ";
        }
        sqlInsert = sqlInsert + bean.getCerHistorial().getId();
        sqlInsert = sqlInsert + " );";
        return sqlInsert;
    }

    /*****************/
    /* Otros Metodos */
    /*****************/

    public Long countByCerHistorial(CerHistorial cerHistorial) {
        return this.cerHistorialDetalleSapDeltaRepository.countByCerHistorial(cerHistorial);
    }

    public GraphBean graphByCerHistorial() {
        List<CerHistorial> lista = this.cerHistorialDeltaRepository.findAll();
        if (lista == null || lista.size() <= 0) {
            return null;
        }
        GraphBean graphBean = new GraphBean();

        /* Obteniendo Pie Chart */
        GraphPieChart graphPieChart = new GraphPieChart();
        GraphDataset graphDataset = new GraphDataset();
        GraphDataset[] listaGraphDataset = new GraphDataset[1];
        Long[] data = new Long[lista.size()];
        String[] labels = new String[lista.size()];
        String[] backgroundColor = new String[lista.size()];
        String[] hoverBackgroundColor = new String[lista.size()];
        String[] borderColor = new String[lista.size()];
        String[] hoverBorderColor = new String[lista.size()];
        int contador = 0;
        int contadorColor = 0;
        for (CerHistorial bean : lista) {
            Long valor = this.cerHistorialDetalleSapDeltaRepository.countByCerHistorial(bean);
            String descripcion = this.setGraphDescripcionByCerHistorial(bean);
            data[contador] = valor;
            labels[contador] = descripcion;
            backgroundColor[contador] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBackgroundColor[contador] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            borderColor[contador] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBorderColor[contador] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            contador++;
            contadorColor++;
            if (contadorColor >= REGISTROS_COLOR) {
                contadorColor = 1;
            }
        }
        String titulo = this.setGraphPieChartTituloByCerHistorial();
        graphDataset.setLabel(titulo);
        graphDataset.setData(data);
        graphDataset.setBackgroundColor(backgroundColor);
        graphDataset.setHoverBackgroundColor(hoverBackgroundColor);
        listaGraphDataset[0] = graphDataset;
        graphPieChart.setDatasets(listaGraphDataset);
        graphPieChart.setLabels(labels);
        graphBean.setPiechart(graphPieChart);

        GraphBarChart graphBarChart = new GraphBarChart();
        GraphDataset[] listaGraphDatasetBar = new GraphDataset[lista.size()];
        String[] labelsBar = new String[1];
        labelsBar[0] = this.setGraphBarChartTituloByCerHistorial();
        contador = 0;
        contadorColor = 0;
        for (CerHistorial bean : lista) {
            Long valor = this.cerHistorialDetalleSapDeltaRepository.countByCerHistorial(bean);
            String descripcion = this.setGraphDescripcionByCerHistorial(bean);
            GraphDataset graphDatasetBar = new GraphDataset();
            Long[] dataBar = new Long[1];

            String[] backgroundColorBar = new String[1];
            String[] hoverBackgroundColorBar = new String[1];
            String[] borderColorBar = new String[1];
            String[] hoverBorderColorBar = new String[1];

            dataBar[0] = valor;
            backgroundColorBar[0] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBackgroundColorBar[0] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            borderColorBar[0] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBorderColorBar[0] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];

            graphDatasetBar.setLabel(descripcion);
            graphDatasetBar.setData(dataBar);
            graphDatasetBar.setBackgroundColor(backgroundColorBar);
            graphDatasetBar.setHoverBackgroundColor(hoverBackgroundColorBar);
            graphDatasetBar.setBorderColor(borderColorBar);
            graphDatasetBar.setHoverBorderColor(hoverBorderColorBar);
            listaGraphDatasetBar[contador] = graphDatasetBar;

            contador++;
            contadorColor++;
            if (contadorColor >= REGISTROS_COLOR) {
                contadorColor = 1;
            }

        }
        graphBarChart.setDatasets(listaGraphDatasetBar);
        graphBarChart.setLabels(labelsBar);
        graphBean.setBarchart(graphBarChart);
        return graphBean;
    }

    protected String setGraphDescripcionByCerHistorial(CerHistorial cerHistorial) {
        return cerHistorial.getId().toString();
    }

    protected String setGraphPieChartTituloByCerHistorial() {
        return "CerHistorial";
    }

    protected String setGraphBarChartTituloByCerHistorial() {
        return "CerHistorial";
    }

}
