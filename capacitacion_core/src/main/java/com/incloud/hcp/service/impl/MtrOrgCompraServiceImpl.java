/*
 * Project home: https://github.com/jaxio/celerio-angular-quickstart
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Modificado por CARLOS BAZALAR
 * Email: cbazalarlarosa@gmail.com
 * Template pack-angular:src/main/java/dto/EntityDTOService.java.e.vm
 */
package com.incloud.hcp.service.impl;

import com.incloud.hcp.common.enums.AppParametriaLabelEnum;
import com.incloud.hcp.common.enums.AppParametriaModuloEnum;
import com.incloud.hcp.common.excel.ExcelDefault;
import com.incloud.hcp.domain.AppParametria;
import com.incloud.hcp.domain.MtrOrgCompra;
import com.incloud.hcp.domain.MtrOrgCompra_;
import com.incloud.hcp.domain.response.MtrOrgCompraResponse;
import com.incloud.hcp.exception.ServiceException;
import com.incloud.hcp.repository.delta.AppParametriaDeltaRepository;
import com.incloud.hcp.repository.delta.MtrOrgCompraDeltaRepository;
import com.incloud.hcp.service.MtrOrgCompraService;
import com.incloud.hcp.service._framework.bean.BeanCargaMasivoDTO;
import com.incloud.hcp.service._framework.bean.BeanListaMasivoDTO;
import com.incloud.hcp.service._framework.impl.JPACustomServiceImpl;
import com.incloud.hcp.service.requireNew.MtrOrgCompraRequireNewService;
import com.incloud.hcp.service.support.PageRequestByExample;
import com.incloud.hcp.utils.Constants;
import com.incloud.hcp.utils.PredicateUtils;
import org.apache.commons.beanutils.BeanUtils;
import org.apache.commons.lang.StringUtils;
import org.apache.poi.hssf.usermodel.HSSFRow;
import org.apache.poi.ss.usermodel.Cell;
import org.apache.poi.ss.usermodel.CellStyle;
import org.apache.poi.ss.usermodel.Row;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.i18n.LocaleContextHolder;
import org.springframework.data.domain.Example;
import org.springframework.data.domain.ExampleMatcher;
import org.springframework.data.domain.Sort;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Propagation;
import org.springframework.transaction.annotation.Transactional;

import javax.persistence.criteria.CriteriaBuilder;
import javax.persistence.criteria.Predicate;
import javax.persistence.criteria.Root;
import java.math.BigDecimal;
import java.util.ArrayList;
import java.util.List;
import java.util.Optional;

/**
 * A simple DTO Facility for MtrOrgCompra.
 */
@Service
@Transactional(propagation = Propagation.REQUIRED, rollbackFor = Exception.class)
public abstract class MtrOrgCompraServiceImpl extends JPACustomServiceImpl<MtrOrgCompraResponse, MtrOrgCompra, Integer> implements MtrOrgCompraService {

    protected final String NAME_SHEET = "MtrOrgCompra";
    protected final String CONFIG_TITLE = "com/incloud/hcp/excel/MtrOrgCompraExcel.xml";
    private final Integer REGISTROS_COLOR = 10;

    @Autowired
    protected AppParametriaDeltaRepository appParametriaDeltaRepository;

    @Autowired
    protected MtrOrgCompraDeltaRepository mtrOrgCompraDeltaRepository;

    @Autowired
    protected MtrOrgCompraRequireNewService mtrOrgCompraRequireNewService;

    /*****************************/
    /* Metodos de Busqueda       */
    /*****************************/

    protected final ExampleMatcher setAbstractFind(ExampleMatcher matcher) {
        matcher = ExampleMatcher.matching() //
                .withMatcher(MtrOrgCompra_.codigoOrgCompraSap.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(MtrOrgCompra_.descripcion.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(MtrOrgCompra_.estado.getName(), match -> match.ignoreCase().startsWith());
        return matcher;
    }

    protected abstract Sort setFindAll(Sort sort);

    protected abstract Sort setFind(MtrOrgCompra req, ExampleMatcher matcher, Example<MtrOrgCompra> example, Sort sort);

    protected abstract void setFindPaginated(PageRequestByExample<MtrOrgCompra> req, ExampleMatcher matcher, Example<MtrOrgCompra> example);

    protected final MtrOrgCompra setObtenerBeanResponse(MtrOrgCompraResponse bean) {
        return bean.getBean();
    }

    protected final Class<MtrOrgCompra> setObtenerClassBean() {
        return MtrOrgCompra.class;
    }

    protected List<Predicate> setAbstractPredicate(MtrOrgCompraResponse bean, CriteriaBuilder cb, Root<MtrOrgCompra> root) {
        List<Predicate> predicates = new ArrayList<>();
        MtrOrgCompra entity = bean.getBean();
        /* Obtener condiciones */
        PredicateUtils.addPredicates(predicates, bean.getIdCondicion(), "id", entity.getId(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getCodigoOrgCompraSapCondicion(), "codigoOrgCompraSap", entity.getCodigoOrgCompraSap(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getDescripcionCondicion(), "descripcion", entity.getDescripcion(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getEstadoCondicion(), "estado", entity.getEstado(), cb, root);
        /* Obtener valores de Lista */
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "id", bean.getIdList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "codigoOrgCompraSap", bean.getCodigoOrgCompraSapList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "descripcion", bean.getDescripcionList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "estado", bean.getEstadoList(), cb, root);
        return predicates;
    }

    /****************/
    /* METODOS CRUD */
    /****************/

    protected final String validacionesPrevias(MtrOrgCompra bean) throws Exception {
        String mensaje = "";
        if (!Optional.of(bean.getCodigoOrgCompraSap()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mtrOrgCompra.codigoOrgCompraSap.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        if (!Optional.of(bean.getDescripcion()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mtrOrgCompra.descripcion.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        if (!Optional.of(bean.getEstado()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mtrOrgCompra.estado.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        String msg = this.setValidacionesPrevias(bean);
        if (StringUtils.isNotBlank(msg)) {
            mensaje += "* " + msg + "<br/>";
        }
        return mensaje;
    }

    protected String setValidacionesPrevias(MtrOrgCompra bean) throws Exception {
        String mensaje = "";
        return mensaje;
    }

    protected String validacionesPreviasCreate(MtrOrgCompra bean) throws Exception {
        String msg = null;
        MtrOrgCompra validar = null;
        validar = this.mtrOrgCompraDeltaRepository.getByCodigoOrgCompraSap(bean.getCodigoOrgCompraSap());
        if (Optional.ofNullable(validar).isPresent()) {
            msg = this.messageSource.getMessage("message.mtrOrgCompra.codigoOrgCompraSap.duplicado", null, LocaleContextHolder.getLocale());
            return msg;
        }
        validar = this.mtrOrgCompraDeltaRepository.getByDescripcion(bean.getDescripcion());
        if (Optional.ofNullable(validar).isPresent()) {
            msg = this.messageSource.getMessage("message.mtrOrgCompra.descripcion.duplicado", null, LocaleContextHolder.getLocale());
            return msg;
        }
        return msg;
    }

    protected String validacionesPreviasSave(MtrOrgCompra bean) throws Exception {
        String msg = null;
        MtrOrgCompra validar = null;
        validar = this.mtrOrgCompraDeltaRepository.getByCodigoOrgCompraSap(bean.getCodigoOrgCompraSap());
        if (Optional.ofNullable(validar).isPresent()) {
            if (bean.getId().intValue() != validar.getId().intValue()) {
                msg = this.messageSource.getMessage("message.mtrOrgCompra.codigoOrgCompraSap.duplicado", null, LocaleContextHolder.getLocale());
                return msg;
            }
        }
        validar = this.mtrOrgCompraDeltaRepository.getByDescripcion(bean.getDescripcion());
        if (Optional.ofNullable(validar).isPresent()) {
            if (bean.getId().intValue() != validar.getId().intValue()) {
                msg = this.messageSource.getMessage("message.mtrOrgCompra.descripcion.duplicado", null, LocaleContextHolder.getLocale());
                return msg;
            }
        }
        return msg;
    }

    protected MtrOrgCompra completarDatosBean(MtrOrgCompra bean) throws Exception {
        BigDecimal data = new BigDecimal(0.00);
        bean = this.setCompletarDatosBean(bean);
        return bean;
    }

    protected MtrOrgCompra setCompletarDatosBean(MtrOrgCompra bean) throws Exception {
        return bean;
    }

    protected final MtrOrgCompra setAbstractCreate(MtrOrgCompra dto) throws Exception {
        MtrOrgCompra bean = new MtrOrgCompra();
        bean = (MtrOrgCompra) BeanUtils.cloneBean(dto);
        return bean;
    }

    /************************/
    /* METODOS CRUD Masivos */
    /************************/

    protected MtrOrgCompra setUploadExcel(Cell currentCell, MtrOrgCompra mtrOrgCompra, int contador) throws Exception {
        Double valorDouble = new Double(0);
        BigDecimal valorDecimal = new BigDecimal(0);
        Boolean valorBoolean = new Boolean(true);
        String valorCadena = "";
        switch (contador) {
        case 1:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 5) {
                    throw new ServiceException("Valor Campo codigoOrgCompraSap contiene mas de 5 caracter(es)");
                }
                mtrOrgCompra.setCodigoOrgCompraSap(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo codigoOrgCompraSap está en formato incorrecto");
            }
            break;
        case 2:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 100) {
                    throw new ServiceException("Valor Campo descripcion contiene mas de 100 caracter(es)");
                }
                mtrOrgCompra.setDescripcion(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo descripcion está en formato incorrecto");
            }
            break;
        case 3:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 1) {
                    throw new ServiceException("Valor Campo estado contiene mas de 1 caracter(es)");
                }
                mtrOrgCompra.setEstado(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo estado está en formato incorrecto");
            }
            break;
        default:
            break;
        }
        return mtrOrgCompra;
    }

    protected AppParametria setObtenerRegistroConfiguracionUploadExcel() {
        AppParametria appParametriaData = this.appParametriaDeltaRepository.getByModuloAndLabelAndStatus(AppParametriaModuloEnum.CARGA_EXCEL.getEstado(),
                AppParametriaLabelEnum.INICIO_REGISTRO_DATA.getEstado(), Constants.ESTADO_ACTIVO);
        return appParametriaData;
    }

    protected MtrOrgCompra setInicializarBeanUpdateExcel() {
        MtrOrgCompra bean = new MtrOrgCompra();
        bean.setId(null);
        return bean;
    }

    protected final Integer setObtenerId(MtrOrgCompra bean) {
        return bean.getId();
    }

    /************************/
    /* Instancia de Bean    */
    /************************/

    protected final MtrOrgCompra createInstance() {
        MtrOrgCompra mtrOrgCompra = new MtrOrgCompra();
        return mtrOrgCompra;
    }

    protected final BeanCargaMasivoDTO<MtrOrgCompra> createInstanceMasivoDTO() {
        BeanCargaMasivoDTO<MtrOrgCompra> beanCargaMasivoDTO = new BeanCargaMasivoDTO<MtrOrgCompra>();
        return beanCargaMasivoDTO;

    }

    protected final BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrOrgCompra>> createInstanceListaMasivoDTO() {
        BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrOrgCompra>> beanCargaMasivoDTOBeanListaMasivoDTO = new BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrOrgCompra>>();
        return beanCargaMasivoDTOBeanListaMasivoDTO;
    }

    /*****************************/
    /* Metodos que generan Excel */
    /*****************************/

    protected String devuelveNombreSheet() {
        return this.NAME_SHEET;
    }

    protected String devuelveListaHeaderExcelXML() {
        return this.CONFIG_TITLE;
    }

    protected int setAbstractDownloadExcel(MtrOrgCompra bean, HSSFRow dataRow) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getCodigoOrgCompraSap(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getDescripcion(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getEstado(), dataRow.createCell(contador));
        contador++;
        return contador;
    }

    protected int setAbstractDownloadExcelSXLSX(MtrOrgCompra bean, Row dataRow, List<CellStyle> cellStyleList) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador), "I", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getCodigoOrgCompraSap(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getDescripcion(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getEstado(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        return contador;
    }

    protected String setAbstractGenerateInsertExcelSXLSX(MtrOrgCompra bean) {
        String fechaS = "";
        String sqlInsert = "INSERT INTO mtr_org_compra(";
        sqlInsert = sqlInsert + "mtr_org_compra_id" + ", ";
        sqlInsert = sqlInsert + "codigo_org_compra_sap" + ", ";
        sqlInsert = sqlInsert + "descripcion" + ", ";
        sqlInsert = sqlInsert + "estado" + ")";
        sqlInsert = sqlInsert + " VALUES (";
        sqlInsert = sqlInsert + bean.getId() + ", ";
        if (StringUtils.isBlank(bean.getCodigoOrgCompraSap())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getCodigoOrgCompraSap() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getDescripcion())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getDescripcion() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getEstado())) {
            sqlInsert = sqlInsert + "null";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getEstado() + "'";
        }
        sqlInsert = sqlInsert + " );";
        return sqlInsert;
    }

    /*****************/
    /* Otros Metodos */
    /*****************/

}
