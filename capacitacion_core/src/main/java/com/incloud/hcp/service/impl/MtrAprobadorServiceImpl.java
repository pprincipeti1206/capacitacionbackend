/*
 * Project home: https://github.com/jaxio/celerio-angular-quickstart
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Modificado por CARLOS BAZALAR
 * Email: cbazalarlarosa@gmail.com
 * Template pack-angular:src/main/java/dto/EntityDTOService.java.e.vm
 */
package com.incloud.hcp.service.impl;

import com.incloud.hcp.common.enums.AppParametriaLabelEnum;
import com.incloud.hcp.common.enums.AppParametriaModuloEnum;
import com.incloud.hcp.common.excel.ExcelDefault;
import com.incloud.hcp.common.graph.GraphBarChart;
import com.incloud.hcp.common.graph.GraphBean;
import com.incloud.hcp.common.graph.GraphDataset;
import com.incloud.hcp.common.graph.GraphPieChart;
import com.incloud.hcp.domain.AppParametria;
import com.incloud.hcp.domain.MtrAprobador;
import com.incloud.hcp.domain.MtrAprobador_;
import com.incloud.hcp.domain.MtrAreaTrabajo;
import com.incloud.hcp.domain.response.MtrAprobadorResponse;
import com.incloud.hcp.exception.ServiceException;
import com.incloud.hcp.repository.delta.AppParametriaDeltaRepository;
import com.incloud.hcp.repository.delta.MtrAprobadorDeltaRepository;
import com.incloud.hcp.repository.delta.MtrAreaTrabajoDeltaRepository;
import com.incloud.hcp.service.MtrAprobadorService;
import com.incloud.hcp.service._framework.bean.BeanCargaMasivoDTO;
import com.incloud.hcp.service._framework.bean.BeanListaMasivoDTO;
import com.incloud.hcp.service._framework.impl.JPACustomServiceImpl;
import com.incloud.hcp.service.delta.MtrAreaTrabajoDeltaService;
import com.incloud.hcp.service.requireNew.MtrAprobadorRequireNewService;
import com.incloud.hcp.service.support.PageRequestByExample;
import com.incloud.hcp.utils.Constants;
import com.incloud.hcp.utils.PredicateUtils;
import org.apache.commons.beanutils.BeanUtils;
import org.apache.commons.lang.StringUtils;
import org.apache.poi.hssf.usermodel.HSSFRow;
import org.apache.poi.ss.usermodel.Cell;
import org.apache.poi.ss.usermodel.CellStyle;
import org.apache.poi.ss.usermodel.Row;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.i18n.LocaleContextHolder;
import org.springframework.data.domain.Example;
import org.springframework.data.domain.ExampleMatcher;
import org.springframework.data.domain.Sort;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Propagation;
import org.springframework.transaction.annotation.Transactional;

import javax.persistence.criteria.CriteriaBuilder;
import javax.persistence.criteria.Predicate;
import javax.persistence.criteria.Root;
import java.math.BigDecimal;
import java.util.ArrayList;
import java.util.List;
import java.util.Optional;

/**
 * A simple DTO Facility for MtrAprobador.
 */
@Service
@Transactional(propagation = Propagation.REQUIRED, rollbackFor = Exception.class)
public abstract class MtrAprobadorServiceImpl extends JPACustomServiceImpl<MtrAprobadorResponse, MtrAprobador, Integer> implements MtrAprobadorService {

    protected final String NAME_SHEET = "MtrAprobador";
    protected final String CONFIG_TITLE = "com/incloud/hcp/excel/MtrAprobadorExcel.xml";
    private final Integer REGISTROS_COLOR = 10;

    @Autowired
    protected AppParametriaDeltaRepository appParametriaDeltaRepository;

    @Autowired
    protected MtrAprobadorDeltaRepository mtrAprobadorDeltaRepository;

    @Autowired
    protected MtrAprobadorRequireNewService mtrAprobadorRequireNewService;

    @Autowired
    protected MtrAreaTrabajoDeltaService mtrAreaTrabajoDeltaService;

    @Autowired
    protected MtrAreaTrabajoDeltaRepository mtrAreaTrabajoDeltaRepository;

    /*****************************/
    /* Metodos de Busqueda       */
    /*****************************/

    protected final ExampleMatcher setAbstractFind(ExampleMatcher matcher) {
        matcher = ExampleMatcher.matching() //
                .withMatcher(MtrAprobador_.aprobadorSap.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(MtrAprobador_.nombre.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(MtrAprobador_.apellidos.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(MtrAprobador_.email.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(MtrAprobador_.estado.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(MtrAprobador_.codigoIdp.getName(), match -> match.ignoreCase().startsWith());
        return matcher;
    }

    protected abstract Sort setFindAll(Sort sort);

    protected abstract Sort setFind(MtrAprobador req, ExampleMatcher matcher, Example<MtrAprobador> example, Sort sort);

    protected abstract void setFindPaginated(PageRequestByExample<MtrAprobador> req, ExampleMatcher matcher, Example<MtrAprobador> example);

    protected final MtrAprobador setObtenerBeanResponse(MtrAprobadorResponse bean) {
        return bean.getBean();
    }

    protected final Class<MtrAprobador> setObtenerClassBean() {
        return MtrAprobador.class;
    }

    protected List<Predicate> setAbstractPredicate(MtrAprobadorResponse bean, CriteriaBuilder cb, Root<MtrAprobador> root) {
        List<Predicate> predicates = new ArrayList<>();
        MtrAprobador entity = bean.getBean();
        /* Obtener condiciones */
        PredicateUtils.addPredicates(predicates, bean.getIdCondicion(), "id", entity.getId(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getAprobadorSapCondicion(), "aprobadorSap", entity.getAprobadorSap(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getNombreCondicion(), "nombre", entity.getNombre(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getApellidosCondicion(), "apellidos", entity.getApellidos(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getEmailCondicion(), "email", entity.getEmail(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getEstadoCondicion(), "estado", entity.getEstado(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getCodigoIdpCondicion(), "codigoIdp", entity.getCodigoIdp(), cb, root);
        //PredicateUtils.addPredicates(predicates, bean.getMtrAreaTrabajoCondicion(), "mtrAreaTrabajo", entity.getMtrAreaTrabajo(), cb, root);
        /* Obtener valores de Lista */
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "id", bean.getIdList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "aprobadorSap", bean.getAprobadorSapList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "nombre", bean.getNombreList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "apellidos", bean.getApellidosList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "email", bean.getEmailList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "estado", bean.getEstadoList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "codigoIdp", bean.getCodigoIdpList(), cb, root);
        //PredicateUtils.addPredicatesListValorBean(predicates, "mtrAreaTrabajo", bean.getMtrAreaTrabajoList(), cb, root);
        return predicates;
    }

    /****************/
    /* METODOS CRUD */
    /****************/

    protected final String validacionesPrevias(MtrAprobador bean) throws Exception {
        String mensaje = "";
        if (!Optional.of(bean.getAprobadorSap()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mtrAprobador.aprobadorSap.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        if (!Optional.of(bean.getNombre()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mtrAprobador.nombre.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        if (!Optional.of(bean.getApellidos()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mtrAprobador.apellidos.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        if (!Optional.of(bean.getEmail()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mtrAprobador.email.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        if (!Optional.of(bean.getEstado()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mtrAprobador.estado.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        String msg = this.setValidacionesPrevias(bean);
        if (StringUtils.isNotBlank(msg)) {
            mensaje += "* " + msg + "<br/>";
        }
        return mensaje;
    }

    protected String setValidacionesPrevias(MtrAprobador bean) throws Exception {
        String mensaje = "";
        return mensaje;
    }

    protected String validacionesPreviasCreate(MtrAprobador bean) throws Exception {
        String msg = null;
        MtrAprobador validar = null;
        validar = this.mtrAprobadorDeltaRepository.getByAprobadorSap(bean.getAprobadorSap());
        if (Optional.ofNullable(validar).isPresent()) {
            msg = this.messageSource.getMessage("message.mtrAprobador.aprobadorSap.duplicado", null, LocaleContextHolder.getLocale());
            return msg;
        }
        validar = this.mtrAprobadorDeltaRepository.getByEmail(bean.getEmail());
        if (Optional.ofNullable(validar).isPresent()) {
            msg = this.messageSource.getMessage("message.mtrAprobador.email.duplicado", null, LocaleContextHolder.getLocale());
            return msg;
        }
        return msg;
    }

    protected String validacionesPreviasSave(MtrAprobador bean) throws Exception {
        String msg = null;
        MtrAprobador validar = null;
        validar = this.mtrAprobadorDeltaRepository.getByAprobadorSap(bean.getAprobadorSap());
        if (Optional.ofNullable(validar).isPresent()) {
            if (bean.getId().intValue() != validar.getId().intValue()) {
                msg = this.messageSource.getMessage("message.mtrAprobador.aprobadorSap.duplicado", null, LocaleContextHolder.getLocale());
                return msg;
            }
        }
        validar = this.mtrAprobadorDeltaRepository.getByEmail(bean.getEmail());
        if (Optional.ofNullable(validar).isPresent()) {
            if (bean.getId().intValue() != validar.getId().intValue()) {
                msg = this.messageSource.getMessage("message.mtrAprobador.email.duplicado", null, LocaleContextHolder.getLocale());
                return msg;
            }
        }
        return msg;
    }

    protected MtrAprobador completarDatosBean(MtrAprobador bean) throws Exception {
        BigDecimal data = new BigDecimal(0.00);
        bean = this.setCompletarDatosBean(bean);
        return bean;
    }

    protected MtrAprobador setCompletarDatosBean(MtrAprobador bean) throws Exception {
        return bean;
    }

    protected final MtrAprobador setAbstractCreate(MtrAprobador dto) throws Exception {
        MtrAprobador bean = new MtrAprobador();
        bean = (MtrAprobador) BeanUtils.cloneBean(dto);
        return bean;
    }

    /************************/
    /* METODOS CRUD Masivos */
    /************************/

    protected MtrAprobador setUploadExcel(Cell currentCell, MtrAprobador mtrAprobador, int contador) throws Exception {
        Double valorDouble = new Double(0);
        BigDecimal valorDecimal = new BigDecimal(0);
        Boolean valorBoolean = new Boolean(true);
        String valorCadena = "";
        switch (contador) {
        case 1:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 100) {
                    throw new ServiceException("Valor Campo aprobadorSap contiene mas de 100 caracter(es)");
                }
                mtrAprobador.setAprobadorSap(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo aprobadorSap está en formato incorrecto");
            }
            break;
        case 2:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 100) {
                    throw new ServiceException("Valor Campo nombre contiene mas de 100 caracter(es)");
                }
                mtrAprobador.setNombre(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo nombre está en formato incorrecto");
            }
            break;
        case 3:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 100) {
                    throw new ServiceException("Valor Campo apellidos contiene mas de 100 caracter(es)");
                }
                mtrAprobador.setApellidos(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo apellidos está en formato incorrecto");
            }
            break;
        case 4:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 255) {
                    throw new ServiceException("Valor Campo email contiene mas de 255 caracter(es)");
                }
                mtrAprobador.setEmail(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo email está en formato incorrecto");
            }
            break;
        case 5:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 1) {
                    throw new ServiceException("Valor Campo estado contiene mas de 1 caracter(es)");
                }
                mtrAprobador.setEstado(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo estado está en formato incorrecto");
            }
            break;
        case 6:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 20) {
                    throw new ServiceException("Valor Campo codigoIdp contiene mas de 20 caracter(es)");
                }
                mtrAprobador.setCodigoIdp(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo codigoIdp está en formato incorrecto");
            }
            break;
        default:
            break;
        }
        return mtrAprobador;
    }

    protected AppParametria setObtenerRegistroConfiguracionUploadExcel() {
        AppParametria appParametriaData = this.appParametriaDeltaRepository.getByModuloAndLabelAndStatus(AppParametriaModuloEnum.CARGA_EXCEL.getEstado(),
                AppParametriaLabelEnum.INICIO_REGISTRO_DATA.getEstado(), Constants.ESTADO_ACTIVO);
        return appParametriaData;
    }

    protected MtrAprobador setInicializarBeanUpdateExcel() {
        MtrAprobador bean = new MtrAprobador();
        bean.setId(null);
        return bean;
    }

    protected final Integer setObtenerId(MtrAprobador bean) {
        return bean.getId();
    }

    /************************/
    /* Instancia de Bean    */
    /************************/

    protected final MtrAprobador createInstance() {
        MtrAprobador mtrAprobador = new MtrAprobador();
        return mtrAprobador;
    }

    protected final BeanCargaMasivoDTO<MtrAprobador> createInstanceMasivoDTO() {
        BeanCargaMasivoDTO<MtrAprobador> beanCargaMasivoDTO = new BeanCargaMasivoDTO<MtrAprobador>();
        return beanCargaMasivoDTO;

    }

    protected final BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrAprobador>> createInstanceListaMasivoDTO() {
        BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrAprobador>> beanCargaMasivoDTOBeanListaMasivoDTO = new BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrAprobador>>();
        return beanCargaMasivoDTOBeanListaMasivoDTO;
    }

    /*****************************/
    /* Metodos que generan Excel */
    /*****************************/

    protected String devuelveNombreSheet() {
        return this.NAME_SHEET;
    }

    protected String devuelveListaHeaderExcelXML() {
        return this.CONFIG_TITLE;
    }

    protected int setAbstractDownloadExcel(MtrAprobador bean, HSSFRow dataRow) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getAprobadorSap(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getNombre(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getApellidos(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getEmail(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getEstado(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getCodigoIdp(), dataRow.createCell(contador));
        contador++;
        return contador;
    }

    protected int setAbstractDownloadExcelSXLSX(MtrAprobador bean, Row dataRow, List<CellStyle> cellStyleList) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador), "I", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getAprobadorSap(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getNombre(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getApellidos(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getEmail(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getEstado(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getCodigoIdp(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        return contador;
    }

    protected String setAbstractGenerateInsertExcelSXLSX(MtrAprobador bean) {
        String fechaS = "";
        String sqlInsert = "INSERT INTO mtr_aprobador(";
        sqlInsert = sqlInsert + "mtr_aprobador_id" + ", ";
        sqlInsert = sqlInsert + "aprobador_sap" + ", ";
        sqlInsert = sqlInsert + "nombre" + ", ";
        sqlInsert = sqlInsert + "apellidos" + ", ";
        sqlInsert = sqlInsert + "email" + ", ";
        sqlInsert = sqlInsert + "estado" + ", ";
        sqlInsert = sqlInsert + "codigo_idp" + ", ";
        sqlInsert = sqlInsert + "mtr_area_trabajo_id" + ")";
        sqlInsert = sqlInsert + " VALUES (";
        sqlInsert = sqlInsert + bean.getId() + ", ";
        if (StringUtils.isBlank(bean.getAprobadorSap())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getAprobadorSap() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getNombre())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getNombre() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getApellidos())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getApellidos() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getEmail())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getEmail() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getEstado())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getEstado() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getCodigoIdp())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getCodigoIdp() + "'" + ", ";
        }
        sqlInsert = sqlInsert + bean.getMtrAreaTrabajo().getId();
        sqlInsert = sqlInsert + " );";
        return sqlInsert;
    }

    /*****************/
    /* Otros Metodos */
    /*****************/

    public Long countByMtrAreaTrabajo(MtrAreaTrabajo mtrAreaTrabajo) {
        return this.mtrAprobadorDeltaRepository.countByMtrAreaTrabajo(mtrAreaTrabajo);
    }

    public GraphBean graphByMtrAreaTrabajo() {
        List<MtrAreaTrabajo> lista = this.mtrAreaTrabajoDeltaRepository.findAll();
        if (lista == null || lista.size() <= 0) {
            return null;
        }
        GraphBean graphBean = new GraphBean();

        /* Obteniendo Pie Chart */
        GraphPieChart graphPieChart = new GraphPieChart();
        GraphDataset graphDataset = new GraphDataset();
        GraphDataset[] listaGraphDataset = new GraphDataset[1];
        Long[] data = new Long[lista.size()];
        String[] labels = new String[lista.size()];
        String[] backgroundColor = new String[lista.size()];
        String[] hoverBackgroundColor = new String[lista.size()];
        String[] borderColor = new String[lista.size()];
        String[] hoverBorderColor = new String[lista.size()];
        int contador = 0;
        int contadorColor = 0;
        for (MtrAreaTrabajo bean : lista) {
            Long valor = this.mtrAprobadorDeltaRepository.countByMtrAreaTrabajo(bean);
            String descripcion = this.setGraphDescripcionByMtrAreaTrabajo(bean);
            data[contador] = valor;
            labels[contador] = descripcion;
            backgroundColor[contador] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBackgroundColor[contador] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            borderColor[contador] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBorderColor[contador] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            contador++;
            contadorColor++;
            if (contadorColor >= REGISTROS_COLOR) {
                contadorColor = 1;
            }
        }
        String titulo = this.setGraphPieChartTituloByMtrAreaTrabajo();
        graphDataset.setLabel(titulo);
        graphDataset.setData(data);
        graphDataset.setBackgroundColor(backgroundColor);
        graphDataset.setHoverBackgroundColor(hoverBackgroundColor);
        listaGraphDataset[0] = graphDataset;
        graphPieChart.setDatasets(listaGraphDataset);
        graphPieChart.setLabels(labels);
        graphBean.setPiechart(graphPieChart);

        GraphBarChart graphBarChart = new GraphBarChart();
        GraphDataset[] listaGraphDatasetBar = new GraphDataset[lista.size()];
        String[] labelsBar = new String[1];
        labelsBar[0] = this.setGraphBarChartTituloByMtrAreaTrabajo();
        contador = 0;
        contadorColor = 0;
        for (MtrAreaTrabajo bean : lista) {
            Long valor = this.mtrAprobadorDeltaRepository.countByMtrAreaTrabajo(bean);
            String descripcion = this.setGraphDescripcionByMtrAreaTrabajo(bean);
            GraphDataset graphDatasetBar = new GraphDataset();
            Long[] dataBar = new Long[1];

            String[] backgroundColorBar = new String[1];
            String[] hoverBackgroundColorBar = new String[1];
            String[] borderColorBar = new String[1];
            String[] hoverBorderColorBar = new String[1];

            dataBar[0] = valor;
            backgroundColorBar[0] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBackgroundColorBar[0] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            borderColorBar[0] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBorderColorBar[0] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];

            graphDatasetBar.setLabel(descripcion);
            graphDatasetBar.setData(dataBar);
            graphDatasetBar.setBackgroundColor(backgroundColorBar);
            graphDatasetBar.setHoverBackgroundColor(hoverBackgroundColorBar);
            graphDatasetBar.setBorderColor(borderColorBar);
            graphDatasetBar.setHoverBorderColor(hoverBorderColorBar);
            listaGraphDatasetBar[contador] = graphDatasetBar;

            contador++;
            contadorColor++;
            if (contadorColor >= REGISTROS_COLOR) {
                contadorColor = 1;
            }

        }
        graphBarChart.setDatasets(listaGraphDatasetBar);
        graphBarChart.setLabels(labelsBar);
        graphBean.setBarchart(graphBarChart);
        return graphBean;
    }

    protected String setGraphDescripcionByMtrAreaTrabajo(MtrAreaTrabajo mtrAreaTrabajo) {
        return mtrAreaTrabajo.getId().toString();
    }

    protected String setGraphPieChartTituloByMtrAreaTrabajo() {
        return "MtrAreaTrabajo";
    }

    protected String setGraphBarChartTituloByMtrAreaTrabajo() {
        return "MtrAreaTrabajo";
    }

}
