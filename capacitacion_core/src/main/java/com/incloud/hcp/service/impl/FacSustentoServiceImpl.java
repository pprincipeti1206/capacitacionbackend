/*
 * Project home: https://github.com/jaxio/celerio-angular-quickstart
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Modificado por CARLOS BAZALAR
 * Email: cbazalarlarosa@gmail.com
 * Template pack-angular:src/main/java/dto/EntityDTOService.java.e.vm
 */
package com.incloud.hcp.service.impl;

import com.incloud.hcp.common.enums.AppParametriaLabelEnum;
import com.incloud.hcp.common.enums.AppParametriaModuloEnum;
import com.incloud.hcp.common.excel.ExcelDefault;
import com.incloud.hcp.domain.AppParametria;
import com.incloud.hcp.domain.FacSustento;
import com.incloud.hcp.domain.FacSustento_;
import com.incloud.hcp.domain.response.FacSustentoResponse;
import com.incloud.hcp.exception.ServiceException;
import com.incloud.hcp.repository.delta.AppParametriaDeltaRepository;
import com.incloud.hcp.repository.delta.FacSustentoDeltaRepository;
import com.incloud.hcp.service.FacSustentoService;
import com.incloud.hcp.service._framework.bean.BeanCargaMasivoDTO;
import com.incloud.hcp.service._framework.bean.BeanListaMasivoDTO;
import com.incloud.hcp.service._framework.impl.JPACustomServiceImpl;
import com.incloud.hcp.service.requireNew.FacSustentoRequireNewService;
import com.incloud.hcp.service.support.PageRequestByExample;
import com.incloud.hcp.utils.Constants;
import com.incloud.hcp.utils.PredicateUtils;
import org.apache.commons.beanutils.BeanUtils;
import org.apache.commons.lang.StringUtils;
import org.apache.poi.hssf.usermodel.HSSFRow;
import org.apache.poi.ss.usermodel.Cell;
import org.apache.poi.ss.usermodel.CellStyle;
import org.apache.poi.ss.usermodel.Row;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.i18n.LocaleContextHolder;
import org.springframework.data.domain.Example;
import org.springframework.data.domain.ExampleMatcher;
import org.springframework.data.domain.Sort;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Propagation;
import org.springframework.transaction.annotation.Transactional;

import javax.persistence.criteria.CriteriaBuilder;
import javax.persistence.criteria.Predicate;
import javax.persistence.criteria.Root;
import java.math.BigDecimal;
import java.util.ArrayList;
import java.util.List;
import java.util.Optional;

/**
 * A simple DTO Facility for FacSustento.
 */
@Service
@Transactional(propagation = Propagation.REQUIRED, rollbackFor = Exception.class)
public abstract class FacSustentoServiceImpl extends JPACustomServiceImpl<FacSustentoResponse, FacSustento, Integer> implements FacSustentoService {

    protected final String NAME_SHEET = "FacSustento";
    protected final String CONFIG_TITLE = "com/incloud/hcp/excel/FacSustentoExcel.xml";
    private final Integer REGISTROS_COLOR = 10;

    @Autowired
    protected AppParametriaDeltaRepository appParametriaDeltaRepository;

    @Autowired
    protected FacSustentoDeltaRepository facSustentoDeltaRepository;

    @Autowired
    protected FacSustentoRequireNewService facSustentoRequireNewService;

    /*****************************/
    /* Metodos de Busqueda       */
    /*****************************/

    protected final ExampleMatcher setAbstractFind(ExampleMatcher matcher) {
        matcher = ExampleMatcher.matching() //
                .withMatcher(FacSustento_.facAnio.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(FacSustento_.facCorrelativo.getName(), match -> match.ignoreCase().startsWith());
        return matcher;
    }

    protected abstract Sort setFindAll(Sort sort);

    protected abstract Sort setFind(FacSustento req, ExampleMatcher matcher, Example<FacSustento> example, Sort sort);

    protected abstract void setFindPaginated(PageRequestByExample<FacSustento> req, ExampleMatcher matcher, Example<FacSustento> example);

    protected final FacSustento setObtenerBeanResponse(FacSustentoResponse bean) {
        return bean.getBean();
    }

    protected final Class<FacSustento> setObtenerClassBean() {
        return FacSustento.class;
    }

    protected List<Predicate> setAbstractPredicate(FacSustentoResponse bean, CriteriaBuilder cb, Root<FacSustento> root) {
        List<Predicate> predicates = new ArrayList<>();
        FacSustento entity = bean.getBean();
        /* Obtener condiciones */
        PredicateUtils.addPredicates(predicates, bean.getIdCondicion(), "id", entity.getId(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getFacAnioCondicion(), "facAnio", entity.getFacAnio(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getFacCorrelativoCondicion(), "facCorrelativo", entity.getFacCorrelativo(), cb, root);
        /* Obtener valores de Lista */
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "id", bean.getIdList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "facAnio", bean.getFacAnioList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "facCorrelativo", bean.getFacCorrelativoList(), cb, root);
        return predicates;
    }

    /****************/
    /* METODOS CRUD */
    /****************/

    protected final String validacionesPrevias(FacSustento bean) throws Exception {
        String mensaje = "";
        if (!Optional.of(bean.getFacAnio()).isPresent()) {
            String msg = this.messageSource.getMessage("message.facSustento.facAnio.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        if (!Optional.of(bean.getFacCorrelativo()).isPresent()) {
            String msg = this.messageSource.getMessage("message.facSustento.facCorrelativo.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        String msg = this.setValidacionesPrevias(bean);
        if (StringUtils.isNotBlank(msg)) {
            mensaje += "* " + msg + "<br/>";
        }
        return mensaje;
    }

    protected String setValidacionesPrevias(FacSustento bean) throws Exception {
        String mensaje = "";
        return mensaje;
    }

    protected String validacionesPreviasCreate(FacSustento bean) throws Exception {
        String msg = null;
        FacSustento validar = null;
        validar = this.facSustentoDeltaRepository.getByFacAnio(bean.getFacAnio());
        if (Optional.ofNullable(validar).isPresent()) {
            msg = this.messageSource.getMessage("message.facSustento.facAnio.duplicado", null, LocaleContextHolder.getLocale());
            return msg;
        }
        return msg;
    }

    protected String validacionesPreviasSave(FacSustento bean) throws Exception {
        String msg = null;
        FacSustento validar = null;
        validar = this.facSustentoDeltaRepository.getByFacAnio(bean.getFacAnio());
        if (Optional.ofNullable(validar).isPresent()) {
            if (bean.getId().intValue() != validar.getId().intValue()) {
                msg = this.messageSource.getMessage("message.facSustento.facAnio.duplicado", null, LocaleContextHolder.getLocale());
                return msg;
            }
        }
        return msg;
    }

    protected FacSustento completarDatosBean(FacSustento bean) throws Exception {
        BigDecimal data = new BigDecimal(0.00);
        bean = this.setCompletarDatosBean(bean);
        return bean;
    }

    protected FacSustento setCompletarDatosBean(FacSustento bean) throws Exception {
        return bean;
    }

    protected final FacSustento setAbstractCreate(FacSustento dto) throws Exception {
        FacSustento bean = new FacSustento();
        bean = (FacSustento) BeanUtils.cloneBean(dto);
        return bean;
    }

    /************************/
    /* METODOS CRUD Masivos */
    /************************/

    protected FacSustento setUploadExcel(Cell currentCell, FacSustento facSustento, int contador) throws Exception {
        Double valorDouble = new Double(0);
        BigDecimal valorDecimal = new BigDecimal(0);
        Boolean valorBoolean = new Boolean(true);
        String valorCadena = "";
        switch (contador) {
        case 1:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 255) {
                    throw new ServiceException("Valor Campo facAnio contiene mas de 255 caracter(es)");
                }
                facSustento.setFacAnio(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo facAnio está en formato incorrecto");
            }
            break;
        case 2:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 255) {
                    throw new ServiceException("Valor Campo facCorrelativo contiene mas de 255 caracter(es)");
                }
                facSustento.setFacCorrelativo(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo facCorrelativo está en formato incorrecto");
            }
            break;
        default:
            break;
        }
        return facSustento;
    }

    protected AppParametria setObtenerRegistroConfiguracionUploadExcel() {
        AppParametria appParametriaData = this.appParametriaDeltaRepository.getByModuloAndLabelAndStatus(AppParametriaModuloEnum.CARGA_EXCEL.getEstado(),
                AppParametriaLabelEnum.INICIO_REGISTRO_DATA.getEstado(), Constants.ESTADO_ACTIVO);
        return appParametriaData;
    }

    protected FacSustento setInicializarBeanUpdateExcel() {
        FacSustento bean = new FacSustento();
        bean.setId(null);
        return bean;
    }

    protected final Integer setObtenerId(FacSustento bean) {
        return bean.getId();
    }

    /************************/
    /* Instancia de Bean    */
    /************************/

    protected final FacSustento createInstance() {
        FacSustento facSustento = new FacSustento();
        return facSustento;
    }

    protected final BeanCargaMasivoDTO<FacSustento> createInstanceMasivoDTO() {
        BeanCargaMasivoDTO<FacSustento> beanCargaMasivoDTO = new BeanCargaMasivoDTO<FacSustento>();
        return beanCargaMasivoDTO;

    }

    protected final BeanListaMasivoDTO<BeanCargaMasivoDTO<FacSustento>> createInstanceListaMasivoDTO() {
        BeanListaMasivoDTO<BeanCargaMasivoDTO<FacSustento>> beanCargaMasivoDTOBeanListaMasivoDTO = new BeanListaMasivoDTO<BeanCargaMasivoDTO<FacSustento>>();
        return beanCargaMasivoDTOBeanListaMasivoDTO;
    }

    /*****************************/
    /* Metodos que generan Excel */
    /*****************************/

    protected String devuelveNombreSheet() {
        return this.NAME_SHEET;
    }

    protected String devuelveListaHeaderExcelXML() {
        return this.CONFIG_TITLE;
    }

    protected int setAbstractDownloadExcel(FacSustento bean, HSSFRow dataRow) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getFacAnio(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getFacCorrelativo(), dataRow.createCell(contador));
        contador++;
        return contador;
    }

    protected int setAbstractDownloadExcelSXLSX(FacSustento bean, Row dataRow, List<CellStyle> cellStyleList) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador), "I", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getFacAnio(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getFacCorrelativo(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        return contador;
    }

    protected String setAbstractGenerateInsertExcelSXLSX(FacSustento bean) {
        String fechaS = "";
        String sqlInsert = "INSERT INTO fac_sustento(";
        sqlInsert = sqlInsert + "fac_sustento_id" + ", ";
        sqlInsert = sqlInsert + "fac_anio" + ", ";
        sqlInsert = sqlInsert + "fac_correlativo" + ")";
        sqlInsert = sqlInsert + " VALUES (";
        sqlInsert = sqlInsert + bean.getId() + ", ";
        if (StringUtils.isBlank(bean.getFacAnio())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getFacAnio() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getFacCorrelativo())) {
            sqlInsert = sqlInsert + "null";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getFacCorrelativo() + "'";
        }
        sqlInsert = sqlInsert + " );";
        return sqlInsert;
    }

    /*****************/
    /* Otros Metodos */
    /*****************/

}
