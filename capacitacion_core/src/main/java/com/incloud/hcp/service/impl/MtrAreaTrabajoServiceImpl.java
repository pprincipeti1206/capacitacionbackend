/*
 * Project home: https://github.com/jaxio/celerio-angular-quickstart
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Modificado por CARLOS BAZALAR
 * Email: cbazalarlarosa@gmail.com
 * Template pack-angular:src/main/java/dto/EntityDTOService.java.e.vm
 */
package com.incloud.hcp.service.impl;

import com.incloud.hcp.common.enums.AppParametriaLabelEnum;
import com.incloud.hcp.common.enums.AppParametriaModuloEnum;
import com.incloud.hcp.common.excel.ExcelDefault;
import com.incloud.hcp.domain.AppParametria;
import com.incloud.hcp.domain.MtrAreaTrabajo;
import com.incloud.hcp.domain.MtrAreaTrabajo_;
import com.incloud.hcp.domain.response.MtrAreaTrabajoResponse;
import com.incloud.hcp.exception.ServiceException;
import com.incloud.hcp.repository.delta.AppParametriaDeltaRepository;
import com.incloud.hcp.repository.delta.MtrAreaTrabajoDeltaRepository;
import com.incloud.hcp.service.MtrAreaTrabajoService;
import com.incloud.hcp.service._framework.bean.BeanCargaMasivoDTO;
import com.incloud.hcp.service._framework.bean.BeanListaMasivoDTO;
import com.incloud.hcp.service._framework.impl.JPACustomServiceImpl;
import com.incloud.hcp.service.requireNew.MtrAreaTrabajoRequireNewService;
import com.incloud.hcp.service.support.PageRequestByExample;
import com.incloud.hcp.utils.Constants;
import com.incloud.hcp.utils.PredicateUtils;
import org.apache.commons.beanutils.BeanUtils;
import org.apache.commons.lang.StringUtils;
import org.apache.poi.hssf.usermodel.HSSFRow;
import org.apache.poi.ss.usermodel.Cell;
import org.apache.poi.ss.usermodel.CellStyle;
import org.apache.poi.ss.usermodel.Row;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.i18n.LocaleContextHolder;
import org.springframework.data.domain.Example;
import org.springframework.data.domain.ExampleMatcher;
import org.springframework.data.domain.Sort;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Propagation;
import org.springframework.transaction.annotation.Transactional;

import javax.persistence.criteria.CriteriaBuilder;
import javax.persistence.criteria.Predicate;
import javax.persistence.criteria.Root;
import java.math.BigDecimal;
import java.util.ArrayList;
import java.util.List;
import java.util.Optional;

/**
 * A simple DTO Facility for MtrAreaTrabajo.
 */
@Service
@Transactional(propagation = Propagation.REQUIRED, rollbackFor = Exception.class)
public abstract class MtrAreaTrabajoServiceImpl extends JPACustomServiceImpl<MtrAreaTrabajoResponse, MtrAreaTrabajo, Integer> implements MtrAreaTrabajoService {

    protected final String NAME_SHEET = "MtrAreaTrabajo";
    protected final String CONFIG_TITLE = "com/incloud/hcp/excel/MtrAreaTrabajoExcel.xml";
    private final Integer REGISTROS_COLOR = 10;

    @Autowired
    protected AppParametriaDeltaRepository appParametriaDeltaRepository;

    @Autowired
    protected MtrAreaTrabajoDeltaRepository mtrAreaTrabajoDeltaRepository;

    @Autowired
    protected MtrAreaTrabajoRequireNewService mtrAreaTrabajoRequireNewService;

    /*****************************/
    /* Metodos de Busqueda       */
    /*****************************/

    protected final ExampleMatcher setAbstractFind(ExampleMatcher matcher) {
        matcher = ExampleMatcher.matching() //
                .withMatcher(MtrAreaTrabajo_.codigoAreaTrabajo.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(MtrAreaTrabajo_.descripcion.getName(), match -> match.ignoreCase().startsWith());
        return matcher;
    }

    protected abstract Sort setFindAll(Sort sort);

    protected abstract Sort setFind(MtrAreaTrabajo req, ExampleMatcher matcher, Example<MtrAreaTrabajo> example, Sort sort);

    protected abstract void setFindPaginated(PageRequestByExample<MtrAreaTrabajo> req, ExampleMatcher matcher, Example<MtrAreaTrabajo> example);

    protected final MtrAreaTrabajo setObtenerBeanResponse(MtrAreaTrabajoResponse bean) {
        return bean.getBean();
    }

    protected final Class<MtrAreaTrabajo> setObtenerClassBean() {
        return MtrAreaTrabajo.class;
    }

    protected List<Predicate> setAbstractPredicate(MtrAreaTrabajoResponse bean, CriteriaBuilder cb, Root<MtrAreaTrabajo> root) {
        List<Predicate> predicates = new ArrayList<>();
        MtrAreaTrabajo entity = bean.getBean();
        /* Obtener condiciones */
        PredicateUtils.addPredicates(predicates, bean.getIdCondicion(), "id", entity.getId(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getCodigoAreaTrabajoCondicion(), "codigoAreaTrabajo", entity.getCodigoAreaTrabajo(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getDescripcionCondicion(), "descripcion", entity.getDescripcion(), cb, root);
        /* Obtener valores de Lista */
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "id", bean.getIdList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "codigoAreaTrabajo", bean.getCodigoAreaTrabajoList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "descripcion", bean.getDescripcionList(), cb, root);
        return predicates;
    }

    /****************/
    /* METODOS CRUD */
    /****************/

    protected final String validacionesPrevias(MtrAreaTrabajo bean) throws Exception {
        String mensaje = "";
        if (!Optional.of(bean.getCodigoAreaTrabajo()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mtrAreaTrabajo.codigoAreaTrabajo.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        if (!Optional.of(bean.getDescripcion()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mtrAreaTrabajo.descripcion.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        String msg = this.setValidacionesPrevias(bean);
        if (StringUtils.isNotBlank(msg)) {
            mensaje += "* " + msg + "<br/>";
        }
        return mensaje;
    }

    protected String setValidacionesPrevias(MtrAreaTrabajo bean) throws Exception {
        String mensaje = "";
        return mensaje;
    }

    protected String validacionesPreviasCreate(MtrAreaTrabajo bean) throws Exception {
        String msg = null;
        MtrAreaTrabajo validar = null;
        validar = this.mtrAreaTrabajoDeltaRepository.getByCodigoAreaTrabajo(bean.getCodigoAreaTrabajo());
        if (Optional.ofNullable(validar).isPresent()) {
            msg = this.messageSource.getMessage("message.mtrAreaTrabajo.codigoAreaTrabajo.duplicado", null, LocaleContextHolder.getLocale());
            return msg;
        }
        validar = this.mtrAreaTrabajoDeltaRepository.getByDescripcion(bean.getDescripcion());
        if (Optional.ofNullable(validar).isPresent()) {
            msg = this.messageSource.getMessage("message.mtrAreaTrabajo.descripcion.duplicado", null, LocaleContextHolder.getLocale());
            return msg;
        }
        return msg;
    }

    protected String validacionesPreviasSave(MtrAreaTrabajo bean) throws Exception {
        String msg = null;
        MtrAreaTrabajo validar = null;
        validar = this.mtrAreaTrabajoDeltaRepository.getByCodigoAreaTrabajo(bean.getCodigoAreaTrabajo());
        if (Optional.ofNullable(validar).isPresent()) {
            if (bean.getId().intValue() != validar.getId().intValue()) {
                msg = this.messageSource.getMessage("message.mtrAreaTrabajo.codigoAreaTrabajo.duplicado", null, LocaleContextHolder.getLocale());
                return msg;
            }
        }
        validar = this.mtrAreaTrabajoDeltaRepository.getByDescripcion(bean.getDescripcion());
        if (Optional.ofNullable(validar).isPresent()) {
            if (bean.getId().intValue() != validar.getId().intValue()) {
                msg = this.messageSource.getMessage("message.mtrAreaTrabajo.descripcion.duplicado", null, LocaleContextHolder.getLocale());
                return msg;
            }
        }
        return msg;
    }

    protected MtrAreaTrabajo completarDatosBean(MtrAreaTrabajo bean) throws Exception {
        BigDecimal data = new BigDecimal(0.00);
        bean = this.setCompletarDatosBean(bean);
        return bean;
    }

    protected MtrAreaTrabajo setCompletarDatosBean(MtrAreaTrabajo bean) throws Exception {
        return bean;
    }

    protected final MtrAreaTrabajo setAbstractCreate(MtrAreaTrabajo dto) throws Exception {
        MtrAreaTrabajo bean = new MtrAreaTrabajo();
        bean = (MtrAreaTrabajo) BeanUtils.cloneBean(dto);
        return bean;
    }

    /************************/
    /* METODOS CRUD Masivos */
    /************************/

    protected MtrAreaTrabajo setUploadExcel(Cell currentCell, MtrAreaTrabajo mtrAreaTrabajo, int contador) throws Exception {
        Double valorDouble = new Double(0);
        BigDecimal valorDecimal = new BigDecimal(0);
        Boolean valorBoolean = new Boolean(true);
        String valorCadena = "";
        switch (contador) {
        case 1:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 10) {
                    throw new ServiceException("Valor Campo codigoAreaTrabajo contiene mas de 10 caracter(es)");
                }
                mtrAreaTrabajo.setCodigoAreaTrabajo(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo codigoAreaTrabajo está en formato incorrecto");
            }
            break;
        case 2:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 100) {
                    throw new ServiceException("Valor Campo descripcion contiene mas de 100 caracter(es)");
                }
                mtrAreaTrabajo.setDescripcion(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo descripcion está en formato incorrecto");
            }
            break;
        default:
            break;
        }
        return mtrAreaTrabajo;
    }

    protected AppParametria setObtenerRegistroConfiguracionUploadExcel() {
        AppParametria appParametriaData = this.appParametriaDeltaRepository.getByModuloAndLabelAndStatus(AppParametriaModuloEnum.CARGA_EXCEL.getEstado(),
                AppParametriaLabelEnum.INICIO_REGISTRO_DATA.getEstado(), Constants.ESTADO_ACTIVO);
        return appParametriaData;
    }

    protected MtrAreaTrabajo setInicializarBeanUpdateExcel() {
        MtrAreaTrabajo bean = new MtrAreaTrabajo();
        bean.setId(null);
        return bean;
    }

    protected final Integer setObtenerId(MtrAreaTrabajo bean) {
        return bean.getId();
    }

    /************************/
    /* Instancia de Bean    */
    /************************/

    protected final MtrAreaTrabajo createInstance() {
        MtrAreaTrabajo mtrAreaTrabajo = new MtrAreaTrabajo();
        return mtrAreaTrabajo;
    }

    protected final BeanCargaMasivoDTO<MtrAreaTrabajo> createInstanceMasivoDTO() {
        BeanCargaMasivoDTO<MtrAreaTrabajo> beanCargaMasivoDTO = new BeanCargaMasivoDTO<MtrAreaTrabajo>();
        return beanCargaMasivoDTO;

    }

    protected final BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrAreaTrabajo>> createInstanceListaMasivoDTO() {
        BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrAreaTrabajo>> beanCargaMasivoDTOBeanListaMasivoDTO = new BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrAreaTrabajo>>();
        return beanCargaMasivoDTOBeanListaMasivoDTO;
    }

    /*****************************/
    /* Metodos que generan Excel */
    /*****************************/

    protected String devuelveNombreSheet() {
        return this.NAME_SHEET;
    }

    protected String devuelveListaHeaderExcelXML() {
        return this.CONFIG_TITLE;
    }

    protected int setAbstractDownloadExcel(MtrAreaTrabajo bean, HSSFRow dataRow) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getCodigoAreaTrabajo(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getDescripcion(), dataRow.createCell(contador));
        contador++;
        return contador;
    }

    protected int setAbstractDownloadExcelSXLSX(MtrAreaTrabajo bean, Row dataRow, List<CellStyle> cellStyleList) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador), "I", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getCodigoAreaTrabajo(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getDescripcion(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        return contador;
    }

    protected String setAbstractGenerateInsertExcelSXLSX(MtrAreaTrabajo bean) {
        String fechaS = "";
        String sqlInsert = "INSERT INTO mtr_area_trabajo(";
        sqlInsert = sqlInsert + "mtr_area_trabajo_id" + ", ";
        sqlInsert = sqlInsert + "codigo_area_trabajo" + ", ";
        sqlInsert = sqlInsert + "descripcion" + ")";
        sqlInsert = sqlInsert + " VALUES (";
        sqlInsert = sqlInsert + bean.getId() + ", ";
        if (StringUtils.isBlank(bean.getCodigoAreaTrabajo())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getCodigoAreaTrabajo() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getDescripcion())) {
            sqlInsert = sqlInsert + "null";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getDescripcion() + "'";
        }
        sqlInsert = sqlInsert + " );";
        return sqlInsert;
    }

    /*****************/
    /* Otros Metodos */
    /*****************/

}
