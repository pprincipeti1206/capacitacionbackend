/*
 * Project home: https://github.com/jaxio/celerio-angular-quickstart
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Modificado por CARLOS BAZALAR
 * Email: cbazalarlarosa@gmail.com
 * Template pack-angular:src/main/java/dto/EntityDTOService.java.e.vm
 */
package com.incloud.hcp.service.impl;

import com.incloud.hcp.common.enums.AppParametriaLabelEnum;
import com.incloud.hcp.common.enums.AppParametriaModuloEnum;
import com.incloud.hcp.common.excel.ExcelDefault;
import com.incloud.hcp.domain.AppParametria;
import com.incloud.hcp.domain.MtrEstado;
import com.incloud.hcp.domain.MtrEstado_;
import com.incloud.hcp.domain.response.MtrEstadoResponse;
import com.incloud.hcp.exception.ServiceException;
import com.incloud.hcp.repository.delta.AppParametriaDeltaRepository;
import com.incloud.hcp.repository.delta.MtrEstadoDeltaRepository;
import com.incloud.hcp.service.MtrEstadoService;
import com.incloud.hcp.service._framework.bean.BeanCargaMasivoDTO;
import com.incloud.hcp.service._framework.bean.BeanListaMasivoDTO;
import com.incloud.hcp.service._framework.impl.JPACustomServiceImpl;
import com.incloud.hcp.service.requireNew.MtrEstadoRequireNewService;
import com.incloud.hcp.service.support.PageRequestByExample;
import com.incloud.hcp.utils.Constants;
import com.incloud.hcp.utils.PredicateUtils;
import org.apache.commons.beanutils.BeanUtils;
import org.apache.commons.lang.StringUtils;
import org.apache.poi.hssf.usermodel.HSSFRow;
import org.apache.poi.ss.usermodel.Cell;
import org.apache.poi.ss.usermodel.CellStyle;
import org.apache.poi.ss.usermodel.Row;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.i18n.LocaleContextHolder;
import org.springframework.data.domain.Example;
import org.springframework.data.domain.ExampleMatcher;
import org.springframework.data.domain.Sort;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Propagation;
import org.springframework.transaction.annotation.Transactional;

import javax.persistence.criteria.CriteriaBuilder;
import javax.persistence.criteria.Predicate;
import javax.persistence.criteria.Root;
import java.math.BigDecimal;
import java.util.ArrayList;
import java.util.List;
import java.util.Optional;

/**
 * A simple DTO Facility for MtrEstado.
 */
@Service
@Transactional(propagation = Propagation.REQUIRED, rollbackFor = Exception.class)
public abstract class MtrEstadoServiceImpl extends JPACustomServiceImpl<MtrEstadoResponse, MtrEstado, Integer> implements MtrEstadoService {

    protected final String NAME_SHEET = "MtrEstado";
    protected final String CONFIG_TITLE = "com/incloud/hcp/excel/MtrEstadoExcel.xml";
    private final Integer REGISTROS_COLOR = 10;

    @Autowired
    protected AppParametriaDeltaRepository appParametriaDeltaRepository;

    @Autowired
    protected MtrEstadoDeltaRepository mtrEstadoDeltaRepository;

    @Autowired
    protected MtrEstadoRequireNewService mtrEstadoRequireNewService;

    /*****************************/
    /* Metodos de Busqueda       */
    /*****************************/

    protected final ExampleMatcher setAbstractFind(ExampleMatcher matcher) {
        matcher = ExampleMatcher.matching() //
                .withMatcher(MtrEstado_.codigoAgrupado.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(MtrEstado_.codigoEstado.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(MtrEstado_.descripcion.getName(), match -> match.ignoreCase().startsWith());
        return matcher;
    }

    protected abstract Sort setFindAll(Sort sort);

    protected abstract Sort setFind(MtrEstado req, ExampleMatcher matcher, Example<MtrEstado> example, Sort sort);

    protected abstract void setFindPaginated(PageRequestByExample<MtrEstado> req, ExampleMatcher matcher, Example<MtrEstado> example);

    protected final MtrEstado setObtenerBeanResponse(MtrEstadoResponse bean) {
        return bean.getBean();
    }

    protected final Class<MtrEstado> setObtenerClassBean() {
        return MtrEstado.class;
    }

    protected List<Predicate> setAbstractPredicate(MtrEstadoResponse bean, CriteriaBuilder cb, Root<MtrEstado> root) {
        List<Predicate> predicates = new ArrayList<>();
        MtrEstado entity = bean.getBean();
        /* Obtener condiciones */
        PredicateUtils.addPredicates(predicates, bean.getIdCondicion(), "id", entity.getId(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getCodigoAgrupadoCondicion(), "codigoAgrupado", entity.getCodigoAgrupado(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getCodigoEstadoCondicion(), "codigoEstado", entity.getCodigoEstado(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getDescripcionCondicion(), "descripcion", entity.getDescripcion(), cb, root);
        /* Obtener valores de Lista */
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "id", bean.getIdList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "codigoAgrupado", bean.getCodigoAgrupadoList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "codigoEstado", bean.getCodigoEstadoList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "descripcion", bean.getDescripcionList(), cb, root);
        return predicates;
    }

    /****************/
    /* METODOS CRUD */
    /****************/

    protected final String validacionesPrevias(MtrEstado bean) throws Exception {
        String mensaje = "";
        if (!Optional.of(bean.getCodigoAgrupado()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mtrEstado.codigoAgrupado.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        if (!Optional.of(bean.getCodigoEstado()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mtrEstado.codigoEstado.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        if (!Optional.of(bean.getDescripcion()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mtrEstado.descripcion.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        String msg = this.setValidacionesPrevias(bean);
        if (StringUtils.isNotBlank(msg)) {
            mensaje += "* " + msg + "<br/>";
        }
        return mensaje;
    }

    protected String setValidacionesPrevias(MtrEstado bean) throws Exception {
        String mensaje = "";
        return mensaje;
    }

    protected String validacionesPreviasCreate(MtrEstado bean) throws Exception {
        String msg = null;
        MtrEstado validar = null;
        return msg;
    }

    protected String validacionesPreviasSave(MtrEstado bean) throws Exception {
        String msg = null;
        MtrEstado validar = null;
        return msg;
    }

    protected MtrEstado completarDatosBean(MtrEstado bean) throws Exception {
        BigDecimal data = new BigDecimal(0.00);
        bean = this.setCompletarDatosBean(bean);
        return bean;
    }

    protected MtrEstado setCompletarDatosBean(MtrEstado bean) throws Exception {
        return bean;
    }

    protected final MtrEstado setAbstractCreate(MtrEstado dto) throws Exception {
        MtrEstado bean = new MtrEstado();
        bean = (MtrEstado) BeanUtils.cloneBean(dto);
        return bean;
    }

    /************************/
    /* METODOS CRUD Masivos */
    /************************/

    protected MtrEstado setUploadExcel(Cell currentCell, MtrEstado mtrEstado, int contador) throws Exception {
        Double valorDouble = new Double(0);
        BigDecimal valorDecimal = new BigDecimal(0);
        Boolean valorBoolean = new Boolean(true);
        String valorCadena = "";
        switch (contador) {
        case 1:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 5) {
                    throw new ServiceException("Valor Campo codigoAgrupado contiene mas de 5 caracter(es)");
                }
                mtrEstado.setCodigoAgrupado(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo codigoAgrupado está en formato incorrecto");
            }
            break;
        case 2:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 5) {
                    throw new ServiceException("Valor Campo codigoEstado contiene mas de 5 caracter(es)");
                }
                mtrEstado.setCodigoEstado(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo codigoEstado está en formato incorrecto");
            }
            break;
        case 3:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 50) {
                    throw new ServiceException("Valor Campo descripcion contiene mas de 50 caracter(es)");
                }
                mtrEstado.setDescripcion(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo descripcion está en formato incorrecto");
            }
            break;
        default:
            break;
        }
        return mtrEstado;
    }

    protected AppParametria setObtenerRegistroConfiguracionUploadExcel() {
        AppParametria appParametriaData = this.appParametriaDeltaRepository.getByModuloAndLabelAndStatus(AppParametriaModuloEnum.CARGA_EXCEL.getEstado(),
                AppParametriaLabelEnum.INICIO_REGISTRO_DATA.getEstado(), Constants.ESTADO_ACTIVO);
        return appParametriaData;
    }

    protected MtrEstado setInicializarBeanUpdateExcel() {
        MtrEstado bean = new MtrEstado();
        bean.setId(null);
        return bean;
    }

    protected final Integer setObtenerId(MtrEstado bean) {
        return bean.getId();
    }

    /************************/
    /* Instancia de Bean    */
    /************************/

    protected final MtrEstado createInstance() {
        MtrEstado mtrEstado = new MtrEstado();
        return mtrEstado;
    }

    protected final BeanCargaMasivoDTO<MtrEstado> createInstanceMasivoDTO() {
        BeanCargaMasivoDTO<MtrEstado> beanCargaMasivoDTO = new BeanCargaMasivoDTO<MtrEstado>();
        return beanCargaMasivoDTO;

    }

    protected final BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrEstado>> createInstanceListaMasivoDTO() {
        BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrEstado>> beanCargaMasivoDTOBeanListaMasivoDTO = new BeanListaMasivoDTO<BeanCargaMasivoDTO<MtrEstado>>();
        return beanCargaMasivoDTOBeanListaMasivoDTO;
    }

    /*****************************/
    /* Metodos que generan Excel */
    /*****************************/

    protected String devuelveNombreSheet() {
        return this.NAME_SHEET;
    }

    protected String devuelveListaHeaderExcelXML() {
        return this.CONFIG_TITLE;
    }

    protected int setAbstractDownloadExcel(MtrEstado bean, HSSFRow dataRow) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getCodigoAgrupado(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getCodigoEstado(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getDescripcion(), dataRow.createCell(contador));
        contador++;
        return contador;
    }

    protected int setAbstractDownloadExcelSXLSX(MtrEstado bean, Row dataRow, List<CellStyle> cellStyleList) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador), "I", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getCodigoAgrupado(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getCodigoEstado(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getDescripcion(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        return contador;
    }

    protected String setAbstractGenerateInsertExcelSXLSX(MtrEstado bean) {
        String fechaS = "";
        String sqlInsert = "INSERT INTO mtr_estado(";
        sqlInsert = sqlInsert + "mtr_estado_id" + ", ";
        sqlInsert = sqlInsert + "codigo_agrupado" + ", ";
        sqlInsert = sqlInsert + "codigo_estado" + ", ";
        sqlInsert = sqlInsert + "descripcion" + ")";
        sqlInsert = sqlInsert + " VALUES (";
        sqlInsert = sqlInsert + bean.getId() + ", ";
        if (StringUtils.isBlank(bean.getCodigoAgrupado())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getCodigoAgrupado() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getCodigoEstado())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getCodigoEstado() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getDescripcion())) {
            sqlInsert = sqlInsert + "null";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getDescripcion() + "'";
        }
        sqlInsert = sqlInsert + " );";
        return sqlInsert;
    }

    /*****************/
    /* Otros Metodos */
    /*****************/

}
