/*
 * Project home: https://github.com/jaxio/celerio-angular-quickstart
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Modificado por CARLOS BAZALAR
 * Email: cbazalarlarosa@gmail.com
 * Template pack-angular:src/main/java/rest/EntityResource.java.e.vm
 */
package com.incloud.hcp.rest;

import com.incloud.hcp.common.BindingErrorsResponse;
import com.incloud.hcp.common.graph.GraphBean;
import com.incloud.hcp.domain.*;
import com.incloud.hcp.domain.response.FacEstrategiaFirmaResponse;
import com.incloud.hcp.repository.delta.FacEstrategiaFirmaDeltaRepository;
import com.incloud.hcp.rest._framework.JPACustomRest;
import com.incloud.hcp.service.delta.FacEstrategiaFirmaDeltaService;
import io.swagger.annotations.ApiOperation;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;

import javax.validation.Valid;
import java.net.URISyntaxException;
import java.util.Optional;

import static org.springframework.http.MediaType.APPLICATION_JSON_VALUE;

public abstract class FacEstrategiaFirmaRest extends JPACustomRest<FacEstrategiaFirmaResponse, FacEstrategiaFirma, Integer> {

    @Autowired
    protected FacEstrategiaFirmaDeltaService facEstrategiaFirmaDeltaService;

    @Autowired
    protected FacEstrategiaFirmaDeltaRepository facEstrategiaFirmaDeltaRepository;

    protected String setObtenerNombreArchivoExcel() {
        return "FacEstrategiaFirma";
    }

    /************************/
    /* Instancia de Bean    */
    /************************/
    protected final FacEstrategiaFirma createInstance() {
        FacEstrategiaFirma facEstrategiaFirma = new FacEstrategiaFirma();
        return facEstrategiaFirma;
    }

    /*****************/
    /* Otros Metodos */
    /*****************/

    /**
    * Contador de registros para el atributo mtrUsuarioFacturacion.
    */
    @ApiOperation(value = "Contador de registros para el atributo mtrUsuarioFacturacion", produces = "application/json")
    @PostMapping(value = "/countByMtrUsuarioFacturacion", produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<Long> countByMtrUsuarioFacturacion(@RequestBody @Valid MtrUsuarioFacturacion mtrUsuarioFacturacion, BindingResult bindingResult)
            throws URISyntaxException {
        BindingErrorsResponse errors = new BindingErrorsResponse();
        HttpHeaders headers = new HttpHeaders();
        if (bindingResult.hasErrors()) {
            errors.addAllErrors(bindingResult);
            if (this.devuelveRuntimeException) {
                throw new RuntimeException(errors.toJSON());
            }
            headers.add("errors", errors.toJSON());
            return new ResponseEntity<>(headers, HttpStatus.BAD_REQUEST);
        }
        log.debug("Ingresando countByMtrUsuarioFacturacion");
        try {
            return Optional.ofNullable(this.facEstrategiaFirmaDeltaService.countByMtrUsuarioFacturacion(mtrUsuarioFacturacion))
                    .map(l -> new ResponseEntity<>(l, HttpStatus.OK)).orElse(new ResponseEntity<>(HttpStatus.NO_CONTENT));
        } catch (Exception e) {
            if (this.devuelveRuntimeException) {
                throw new RuntimeException(e);
            }
            headers = this.devuelveErrorHeaders(e);
            return new ResponseEntity<>(headers, HttpStatus.BAD_REQUEST);
        }
    }

    /**
    * Genera Grafico de registros para el atributo mtrUsuarioFacturacion.
    */
    @ApiOperation(value = "Genera Grafico de registros para el atributo mtrUsuarioFacturacion", produces = "application/json")
    @GetMapping(value = "/graphByMtrUsuarioFacturacion", produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<GraphBean> graphByMtrUsuarioFacturacion() throws URISyntaxException {
        log.debug("Ingresando graphByMtrUsuarioFacturacion");
        try {
            return Optional.ofNullable(this.facEstrategiaFirmaDeltaService.graphByMtrUsuarioFacturacion()).map(l -> new ResponseEntity<>(l, HttpStatus.OK))
                    .orElse(new ResponseEntity<>(HttpStatus.NO_CONTENT));
        } catch (Exception e) {
            if (this.devuelveRuntimeException) {
                throw new RuntimeException(e);
            }
            HttpHeaders headers = this.devuelveErrorHeaders(e);
            return new ResponseEntity<>(headers, HttpStatus.BAD_REQUEST);
        }
    }

    /**
    * Contador de registros para el atributo mtrEstrategiaFacturacionFirma.
    */
    @ApiOperation(value = "Contador de registros para el atributo mtrEstrategiaFacturacionFirma", produces = "application/json")
    @PostMapping(value = "/countByMtrEstrategiaFacturacionFirma", produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<Long> countByMtrEstrategiaFacturacionFirma(@RequestBody @Valid MtrEstrategiaFacturacionFirma mtrEstrategiaFacturacionFirma,
            BindingResult bindingResult) throws URISyntaxException {
        BindingErrorsResponse errors = new BindingErrorsResponse();
        HttpHeaders headers = new HttpHeaders();
        if (bindingResult.hasErrors()) {
            errors.addAllErrors(bindingResult);
            if (this.devuelveRuntimeException) {
                throw new RuntimeException(errors.toJSON());
            }
            headers.add("errors", errors.toJSON());
            return new ResponseEntity<>(headers, HttpStatus.BAD_REQUEST);
        }
        log.debug("Ingresando countByMtrEstrategiaFacturacionFirma");
        try {
            return Optional.ofNullable(this.facEstrategiaFirmaDeltaService.countByMtrEstrategiaFacturacionFirma(mtrEstrategiaFacturacionFirma))
                    .map(l -> new ResponseEntity<>(l, HttpStatus.OK)).orElse(new ResponseEntity<>(HttpStatus.NO_CONTENT));
        } catch (Exception e) {
            if (this.devuelveRuntimeException) {
                throw new RuntimeException(e);
            }
            headers = this.devuelveErrorHeaders(e);
            return new ResponseEntity<>(headers, HttpStatus.BAD_REQUEST);
        }
    }

    /**
    * Genera Grafico de registros para el atributo mtrEstrategiaFacturacionFirma.
    */
    @ApiOperation(value = "Genera Grafico de registros para el atributo mtrEstrategiaFacturacionFirma", produces = "application/json")
    @GetMapping(value = "/graphByMtrEstrategiaFacturacionFirma", produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<GraphBean> graphByMtrEstrategiaFacturacionFirma() throws URISyntaxException {
        log.debug("Ingresando graphByMtrEstrategiaFacturacionFirma");
        try {
            return Optional.ofNullable(this.facEstrategiaFirmaDeltaService.graphByMtrEstrategiaFacturacionFirma())
                    .map(l -> new ResponseEntity<>(l, HttpStatus.OK)).orElse(new ResponseEntity<>(HttpStatus.NO_CONTENT));
        } catch (Exception e) {
            if (this.devuelveRuntimeException) {
                throw new RuntimeException(e);
            }
            HttpHeaders headers = this.devuelveErrorHeaders(e);
            return new ResponseEntity<>(headers, HttpStatus.BAD_REQUEST);
        }
    }

    /**
    * Contador de registros para el atributo facEstrategiaIteracion.
    */
    @ApiOperation(value = "Contador de registros para el atributo facEstrategiaIteracion", produces = "application/json")
    @PostMapping(value = "/countByFacEstrategiaIteracion", produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<Long> countByFacEstrategiaIteracion(@RequestBody @Valid FacEstrategiaIteracion facEstrategiaIteracion, BindingResult bindingResult)
            throws URISyntaxException {
        BindingErrorsResponse errors = new BindingErrorsResponse();
        HttpHeaders headers = new HttpHeaders();
        if (bindingResult.hasErrors()) {
            errors.addAllErrors(bindingResult);
            if (this.devuelveRuntimeException) {
                throw new RuntimeException(errors.toJSON());
            }
            headers.add("errors", errors.toJSON());
            return new ResponseEntity<>(headers, HttpStatus.BAD_REQUEST);
        }
        log.debug("Ingresando countByFacEstrategiaIteracion");
        try {
            return Optional.ofNullable(this.facEstrategiaFirmaDeltaService.countByFacEstrategiaIteracion(facEstrategiaIteracion))
                    .map(l -> new ResponseEntity<>(l, HttpStatus.OK)).orElse(new ResponseEntity<>(HttpStatus.NO_CONTENT));
        } catch (Exception e) {
            if (this.devuelveRuntimeException) {
                throw new RuntimeException(e);
            }
            headers = this.devuelveErrorHeaders(e);
            return new ResponseEntity<>(headers, HttpStatus.BAD_REQUEST);
        }
    }

    /**
    * Genera Grafico de registros para el atributo facEstrategiaIteracion.
    */
    @ApiOperation(value = "Genera Grafico de registros para el atributo facEstrategiaIteracion", produces = "application/json")
    @GetMapping(value = "/graphByFacEstrategiaIteracion", produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<GraphBean> graphByFacEstrategiaIteracion() throws URISyntaxException {
        log.debug("Ingresando graphByFacEstrategiaIteracion");
        try {
            return Optional.ofNullable(this.facEstrategiaFirmaDeltaService.graphByFacEstrategiaIteracion()).map(l -> new ResponseEntity<>(l, HttpStatus.OK))
                    .orElse(new ResponseEntity<>(HttpStatus.NO_CONTENT));
        } catch (Exception e) {
            if (this.devuelveRuntimeException) {
                throw new RuntimeException(e);
            }
            HttpHeaders headers = this.devuelveErrorHeaders(e);
            return new ResponseEntity<>(headers, HttpStatus.BAD_REQUEST);
        }
    }

    /**
    * Contador de registros para el atributo mtrSector.
    */
    @ApiOperation(value = "Contador de registros para el atributo mtrSector", produces = "application/json")
    @PostMapping(value = "/countByMtrSector", produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<Long> countByMtrSector(@RequestBody @Valid MtrSector mtrSector, BindingResult bindingResult) throws URISyntaxException {
        BindingErrorsResponse errors = new BindingErrorsResponse();
        HttpHeaders headers = new HttpHeaders();
        if (bindingResult.hasErrors()) {
            errors.addAllErrors(bindingResult);
            if (this.devuelveRuntimeException) {
                throw new RuntimeException(errors.toJSON());
            }
            headers.add("errors", errors.toJSON());
            return new ResponseEntity<>(headers, HttpStatus.BAD_REQUEST);
        }
        log.debug("Ingresando countByMtrSector");
        try {
            return Optional.ofNullable(this.facEstrategiaFirmaDeltaService.countByMtrSector(mtrSector)).map(l -> new ResponseEntity<>(l, HttpStatus.OK))
                    .orElse(new ResponseEntity<>(HttpStatus.NO_CONTENT));
        } catch (Exception e) {
            if (this.devuelveRuntimeException) {
                throw new RuntimeException(e);
            }
            headers = this.devuelveErrorHeaders(e);
            return new ResponseEntity<>(headers, HttpStatus.BAD_REQUEST);
        }
    }

    /**
    * Genera Grafico de registros para el atributo mtrSector.
    */
    @ApiOperation(value = "Genera Grafico de registros para el atributo mtrSector", produces = "application/json")
    @GetMapping(value = "/graphByMtrSector", produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<GraphBean> graphByMtrSector() throws URISyntaxException {
        log.debug("Ingresando graphByMtrSector");
        try {
            return Optional.ofNullable(this.facEstrategiaFirmaDeltaService.graphByMtrSector()).map(l -> new ResponseEntity<>(l, HttpStatus.OK))
                    .orElse(new ResponseEntity<>(HttpStatus.NO_CONTENT));
        } catch (Exception e) {
            if (this.devuelveRuntimeException) {
                throw new RuntimeException(e);
            }
            HttpHeaders headers = this.devuelveErrorHeaders(e);
            return new ResponseEntity<>(headers, HttpStatus.BAD_REQUEST);
        }
    }

}
