/*
 * Project home: https://github.com/jaxio/celerio-angular-quickstart
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Modificado por CARLOS BAZALAR
 * Email: cbazalarlarosa@gmail.com
 * Template pack-angular:src/main/java/rest/EntityResource.java.e.vm
 */
package com.incloud.hcp.rest;

import com.incloud.hcp.common.BindingErrorsResponse;
import com.incloud.hcp.common.graph.GraphBean;
import com.incloud.hcp.domain.CerCertificado;
import com.incloud.hcp.domain.CerCertificadoFirma;
import com.incloud.hcp.domain.CerFirma;
import com.incloud.hcp.domain.response.CerCertificadoFirmaResponse;
import com.incloud.hcp.repository.delta.CerCertificadoFirmaDeltaRepository;
import com.incloud.hcp.rest._framework.JPACustomRest;
import com.incloud.hcp.service.delta.CerCertificadoFirmaDeltaService;
import io.swagger.annotations.ApiOperation;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;

import javax.validation.Valid;
import java.net.URISyntaxException;
import java.util.Optional;

import static org.springframework.http.MediaType.APPLICATION_JSON_VALUE;

public abstract class CerCertificadoFirmaRest extends JPACustomRest<CerCertificadoFirmaResponse, CerCertificadoFirma, Integer> {

    @Autowired
    protected CerCertificadoFirmaDeltaService cerCertificadoFirmaDeltaService;

    @Autowired
    protected CerCertificadoFirmaDeltaRepository cerCertificadoFirmaDeltaRepository;

    protected String setObtenerNombreArchivoExcel() {
        return "CerCertificadoFirma";
    }

    /************************/
    /* Instancia de Bean    */
    /************************/
    protected final CerCertificadoFirma createInstance() {
        CerCertificadoFirma cerCertificadoFirma = new CerCertificadoFirma();
        return cerCertificadoFirma;
    }

    /*****************/
    /* Otros Metodos */
    /*****************/

    /**
    * Contador de registros para el atributo cerCertificado.
    */
    @ApiOperation(value = "Contador de registros para el atributo cerCertificado", produces = "application/json")
    @PostMapping(value = "/countByCerCertificado", produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<Long> countByCerCertificado(@RequestBody @Valid CerCertificado cerCertificado, BindingResult bindingResult)
            throws URISyntaxException {
        BindingErrorsResponse errors = new BindingErrorsResponse();
        HttpHeaders headers = new HttpHeaders();
        if (bindingResult.hasErrors()) {
            errors.addAllErrors(bindingResult);
            if (this.devuelveRuntimeException) {
                throw new RuntimeException(errors.toJSON());
            }
            headers.add("errors", errors.toJSON());
            return new ResponseEntity<>(headers, HttpStatus.BAD_REQUEST);
        }
        log.debug("Ingresando countByCerCertificado");
        try {
            return Optional.ofNullable(this.cerCertificadoFirmaDeltaService.countByCerCertificado(cerCertificado))
                    .map(l -> new ResponseEntity<>(l, HttpStatus.OK)).orElse(new ResponseEntity<>(HttpStatus.NO_CONTENT));
        } catch (Exception e) {
            if (this.devuelveRuntimeException) {
                throw new RuntimeException(e);
            }
            headers = this.devuelveErrorHeaders(e);
            return new ResponseEntity<>(headers, HttpStatus.BAD_REQUEST);
        }
    }

    /**
    * Genera Grafico de registros para el atributo cerCertificado.
    */
    @ApiOperation(value = "Genera Grafico de registros para el atributo cerCertificado", produces = "application/json")
    @GetMapping(value = "/graphByCerCertificado", produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<GraphBean> graphByCerCertificado() throws URISyntaxException {
        log.debug("Ingresando graphByCerCertificado");
        try {
            return Optional.ofNullable(this.cerCertificadoFirmaDeltaService.graphByCerCertificado()).map(l -> new ResponseEntity<>(l, HttpStatus.OK))
                    .orElse(new ResponseEntity<>(HttpStatus.NO_CONTENT));
        } catch (Exception e) {
            if (this.devuelveRuntimeException) {
                throw new RuntimeException(e);
            }
            HttpHeaders headers = this.devuelveErrorHeaders(e);
            return new ResponseEntity<>(headers, HttpStatus.BAD_REQUEST);
        }
    }

    /**
    * Contador de registros para el atributo cerFirma.
    */
    @ApiOperation(value = "Contador de registros para el atributo cerFirma", produces = "application/json")
    @PostMapping(value = "/countByCerFirma", produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<Long> countByCerFirma(@RequestBody @Valid CerFirma cerFirma, BindingResult bindingResult) throws URISyntaxException {
        BindingErrorsResponse errors = new BindingErrorsResponse();
        HttpHeaders headers = new HttpHeaders();
        if (bindingResult.hasErrors()) {
            errors.addAllErrors(bindingResult);
            if (this.devuelveRuntimeException) {
                throw new RuntimeException(errors.toJSON());
            }
            headers.add("errors", errors.toJSON());
            return new ResponseEntity<>(headers, HttpStatus.BAD_REQUEST);
        }
        log.debug("Ingresando countByCerFirma");
        try {
            return Optional.ofNullable(this.cerCertificadoFirmaDeltaService.countByCerFirma(cerFirma)).map(l -> new ResponseEntity<>(l, HttpStatus.OK))
                    .orElse(new ResponseEntity<>(HttpStatus.NO_CONTENT));
        } catch (Exception e) {
            if (this.devuelveRuntimeException) {
                throw new RuntimeException(e);
            }
            headers = this.devuelveErrorHeaders(e);
            return new ResponseEntity<>(headers, HttpStatus.BAD_REQUEST);
        }
    }

    /**
    * Genera Grafico de registros para el atributo cerFirma.
    */
    @ApiOperation(value = "Genera Grafico de registros para el atributo cerFirma", produces = "application/json")
    @GetMapping(value = "/graphByCerFirma", produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<GraphBean> graphByCerFirma() throws URISyntaxException {
        log.debug("Ingresando graphByCerFirma");
        try {
            return Optional.ofNullable(this.cerCertificadoFirmaDeltaService.graphByCerFirma()).map(l -> new ResponseEntity<>(l, HttpStatus.OK))
                    .orElse(new ResponseEntity<>(HttpStatus.NO_CONTENT));
        } catch (Exception e) {
            if (this.devuelveRuntimeException) {
                throw new RuntimeException(e);
            }
            HttpHeaders headers = this.devuelveErrorHeaders(e);
            return new ResponseEntity<>(headers, HttpStatus.BAD_REQUEST);
        }
    }

}
